---
timezone: UTC+8
---

# Zhuofei Lou

**GitHub ID:** BRabb1t

**Telegram:** @fly945

## Self-introduction

Web3 实习计划 2025 冬季实习生

## Notes

<!-- Content_START -->
# 2026-01-23
<!-- DAILY_CHECKIN_2026-01-23_START -->
\### Ethers.js 脚本与交互基础

今天视频主要介绍的是 Node.js 环境下来和链上进行交互

主要使用的核心库是 Ethers.js

\#### Providers 提供者

Provider 是你通往区块链的“只读”连接。它类似于 Web2 中的 API 客户端（如 Firebase SDK）。你不需要自己跑节点，可以连接到 Infura, Alchemy 等服务商提供的节点（类似于连接云数据库）

它能够获取区块号、查询余额、解析 ENS 域名、读取合约状态

\#### 大数的处理

以太坊中的数字（Wei）大到 JavaScript 的原生 `number` 类型无法承载（会溢出）。

Ethers.js 使用 `BigNumber` 对象来安全地存储和操作这些数字。

**转换工具**：

    ◦ `ethers.utils.formatEther(bn)`: 将 BigNumber (Wei) 转换为人类可读的字符串 (Ether)。

    ◦ `ethers.utils.parseEther("1.0")`: 将字符串 (Ether) 转换为 BigNumber (Wei) 用于交易。

• **数学运算**：\*\*绝对不要\*\*把 BigNumber 转成 JS number 去做加减乘除。必须使用 BigNumber 自带的方法，如 `.add()`, `.sub()`, `.mul()`。

\#### 需要注意的地方

**私钥格式**：有些库导出的私钥带 `0x` 前缀，有些不带。如果报错，检查一下是否需要去掉或加上 `0x`。

**网络混淆 (Mainnet vs Testnet)**：

    ◦ **场景**：你想在 Rinkeby 测试网给 "sanfordstout.eth" 转账。

    ◦ **问题**：ENS 注册表通常只部署在 Mainnet 上。如果你只连接了 Rinkeby Provider，你是解析不了 ENS 域名的。

    ◦ **解决**：你需要\*\*两个 Provider\*\*。一个连接 Mainnet 用来解析 ENS 获取地址，另一个连接 Rinkeby 用来发送交易。

**空地址陷阱**：

    ◦ 如果不小心把交易发给了一个 `null` 或空地址，EVM 可能会将其视为\*\*合约部署\*\*操作，这会导致莫名其妙的 Contract Creation。
<!-- DAILY_CHECKIN_2026-01-23_END -->

# 2026-01-22
<!-- DAILY_CHECKIN_2026-01-22_START -->

## 智能合约的主要优势：自动触发、减少中介、减少误差

以太坊是匿名的，但是不是真正的匿名，交易记录完全公开、可追踪，通过交易所 KYC 可追溯

专门的隐私协议（混币、隐私池、零知识证明）

## 典型用例包括但不限于：

\- **铸造与管理货币/代币**：

\- ERC-20、ERC-721、ERC-1155 及各种自定义标准；

\- **DeFi 协议**：

\- 借贷、DEX、衍生品、稳定币、收益聚合等；

\- **NFT & 游戏道具**：

\- 铸造、交易、盲盒、链游内经济系统；

\- **DAO & 治理**：

\- 投票合约、国库管理、多签和 Timelock；

\- **数据存储与访问控制**：

\- 白名单、权限控制、访问日志、License 管理；

\- **链上自动结算与清算机器人**：

\- 定时任务、自动再平衡、预言机驱动逻辑；

\- **结合零知识证明的隐私/验证逻辑**：

\- 不泄露具体数据，只证明“满足某个条件”。

## ABI 应用二进制接口 Application Binary Interface

一个 JSON（JavaScript Object Notation）格式的文件，它描述了智能合约的公共接口。它包含了合约中所有公共函数和事件的详细信息，包括它们的名称、参数类型、返回值类型以及它们的可见性（public、external）。

## 合约元数据（Contract Metadata）

定义：这是一个 JSON 文件，包含了关于合约的额外信息，例如：

\- 编译器版本和设置

\- 源代码路径或其 IPFS / Swarm 等去中心化存储的引用

\- ABI

\- NatSpec 文档（如果在代码里写了）

字节码是部署到区块链上的程序本体

ABI 是你的合约如何交互的说明书
<!-- DAILY_CHECKIN_2026-01-22_END -->

# 2026-01-21
<!-- DAILY_CHECKIN_2026-01-21_START -->


\## 1. Uniswap v2

会上的材料：[https://docs.google.com/presentation/d/12jWBmU08H8L6961tD6gg28fTBS8y6m9FOzJozWwi4-A/edit?usp=sharing](https://docs.google.com/presentation/d/12jWBmU08H8L6961tD6gg28fTBS8y6m9FOzJozWwi4-A/edit?usp=sharing)

听完这个分享，第一个问题还是很吸引我的，也解决了我对于之前往 StandX 里加 USD 池子的疑惑

为什么会出现 v2、v3 相同代币对不一眼的版本，还有旁边的一系列指标代表的是什么

!\[\[CleanShot 2026-01-21 at 23.20.29@2x.png\]\]

\### 1) 基础规则（忽略手续费）

\- 池子里有两种资产储备：

\- $x$：ETH 储备量（横轴）

\- $y$：USDC 储备量（纵轴）

\- 恒定乘积做市商（Constant Product AMM）：

\- $x\\cdot y=k$

\- 任意交易后仍需满足：$x\_{\\text{new}}\\cdot y\_{\\text{new}}=k$

\---

\### 2) 池内价格从哪里来

\- 池内“ETH 的价格（以 USDC 计价）”常用近似为：

\- $P \\approx \\dfrac{y}{x}$（单位：USDC/ETH）

\- 直觉：

\- $y$ 越多、$x$ 越少，则 $\\dfrac{y}{x}$ 越大，ETH 在池内越“贵”。

\> 注：更严格地说，边际价格是曲线在该点的斜率；恒定乘积下边际价格也等于 $y/x$（无手续费时）。

\---

\### 3) 图中初始状态

\- 初始储备：

\- $x\_0=2\\ \\text{ETH}$

\- $y\_0=6000\\ \\text{USDC}$

\- 恒定乘积：

\- $k=x\_0\\cdot y\_0=2\\cdot 6000=12000$

\- 初始池内价格：

\- $P\_0=\\dfrac{y\_0}{x\_0}=\\dfrac{6000}{2}=3000\\ \\text{USDC/ETH}$

\---

\### 4) 外部价为 3200 时，套利方向是什么

\- 外部市场价：$P\_{\\text{ext}}=3200$

\- 池内价格：$P\_0=3000$

\- 因为 $P\_{\\text{ext}} > P\_0$，说明池内 ETH 更便宜（相对外部市场）。

\- 套利者的行为：

1) 在池子里用 USDC 买 ETH（USDC $\\rightarrow$ ETH）

2) 去外部市场按 $3200$ 卖出 ETH

\- 这会导致池子储备变化方向：

\- $y$ 增加（套利者把 USDC 打进池子）

\- $x$ 减少（套利者从池子取走 ETH）

\- 因此 $\\dfrac{y}{x}$ 上升，直到接近 $3200$（无手续费时可推到无套利点）

\---

\### 5) 计算：买多少才能把池内价格推到 $3200$（无套利点）

目标状态 $(x\_1,y\_1)$ 同时满足：

1) 价格对齐：$\\dfrac{y\_1}{x\_1}=3200$

2) 恒定乘积：$x\_1\\cdot y\_1 = k = 12000$

由 (2) 得：$x\_1=\\dfrac{12000}{y\_1}$，代入 (1)：

\- $\\dfrac{y\_1}{12000/y\_1}=3200$

\- $\\dfrac{y\_1^2}{12000}=3200$

\- $y\_1^2 = 3200\\cdot 12000 = 38{,}400{,}000$

\- $y\_1=\\sqrt{38{,}400{,}000}\\approx 6196.77$

因此需要投入的 USDC：

\- $\\Delta y = y\_1-y\_0 = 6196.77-6000 \\approx 196.77\\ \\text{USDC}$

此时池子 ETH 储备：

\- $x\_1=\\dfrac{12000}{6196.77}\\approx 1.93649\\ \\text{ETH}$

从池子里拿到的 ETH：

\- $\\Delta x = x\_0-x\_1 = 2-1.93649 \\approx 0.06351\\ \\text{ETH}$

\---

\### 6) 对应题目选项（以及平均成交价）

\- 这笔套利交易在池子里等价于：

\- 用 $\\Delta y \\approx 196.77\\ \\text{USDC}$ 换出 $\\Delta x \\approx 0.06351\\ \\text{ETH}$

\- 平均成交价（用“总付出/总得到”计算）：

\- $\\bar P=\\dfrac{\\Delta y}{\\Delta x}\\approx \\dfrac{196.77}{0.06351}\\approx 3098.39\\ \\text{USDC/ETH}$

结论：对应选项为 **b)**（平均价介于 $3000$ 与 $3200$ 之间，符合“沿曲线滑点成交”的直觉）。

\---

\### Uniswap V3

在 v2（恒定乘积 $x⋅y=k$）里，LP 的资金会沿着 **0 到 ∞ 的全价格区间**铺开。结果是：

大多数资金实际上落在“永远用不到的价格区间”，资本效率偏低。

这也就是我在加 v3 池子的时候上下限选择的地方

v3 允许 LP 只在一个价格区间内提供流动性，例如只在 **\[2800, 3400\] USDC/ETH** 这段范围做市。

\- 价格在区间内：你的资金参与交易、赚手续费

\- 价格跑出区间：你的仓位会变成\*\*单边资产\*\*（只剩 ETH 或只剩 USDC），不再赚该池交易手续费，直到价格回到区间
<!-- DAILY_CHECKIN_2026-01-21_END -->

# 2026-01-20
<!-- DAILY_CHECKIN_2026-01-20_START -->



## 1\. Web3 实习手册 ｜ 智能合约开发

### 架构差异 去中心化应用 DApp

DApp (Decentralized App) 不像我们熟悉的 `Client -> Server -> Database` 架构。它更像是一个由多个分布式组件拼装的系统。

**前端 (Frontend)**：依然是 HTML/React/Vue，但它\*\*不直接请求后端 API\*\*。它通过“钱包”（如 MetaMask）注入的 Provider 或 RPC 节点与区块链交互。

**智能合约 (Smart Contract)**：这就是我们的“后端”。它定义业务逻辑，部署在 EVM（以太坊虚拟机）上。逻辑一旦部署，\*\*不可篡改\*\*。

**数据检索器 (Indexer)**：这是 Web3 特有的。因为直接在链上查询复杂数据（比如“查询某用户所有的 NFT”）非常慢且难，所以我们需要一个“中间件”（如 The Graph/Ponder），监听链上事件 (Events)，把数据同步到传统数据库（PostgreSQL）供前端查询。

**存储 (Storage)**：大文件（图片、文档）不存链上（太贵），而是存去中心化存储（IPFS/Arweave）。

这与我熟悉的 Java 后端的不同：

\- **接口调用**：以前我们写 `Controller` 给前端调接口；现在前端直接调链上的 `Function`。

\- **数据库观念**：以前我们有 CRUD（增删改查）；在区块链上，\*\*Read\*\* 是免费的（通过 RPC），但 **Write**（交易）是要花钱的（Gas 费），而且非常昂贵。所以我们不能像操作 MySQL 那样随意写入数据，必须“省着写”。

### 为什么需要 RPC 网关

区块链本质上是一个由成千上万台计算机（节点）组成的 **P2P 网络**。这些节点之间通过一种复杂的 P2P 协议（Gossip Protocol）同步数据。

前端网页（JS）并不懂这种 P2P 协议。你没办法直接通过 TCP 连接去“加入”这个网络。

你需要一个“中间人”，它既是区块链网络的一员（存了全量数据），又能听懂 HTTP 请求。这个中间人就是 **RPC 节点**。

它有什么作用呢？

• **读取数据 (eth\_call)**： 当你查询余额时，你其实是发了一个 HTTP 请求给 RPC 节点。RPC 节点本地就存了区块链的“账本副本”，所以它\*\*直接查本地数据库\*\*返回给你，不需要去问全网。这也是为什么读数据是\*\*免费\*\*且\*\*快\*\*的。

• **写入数据 (eth\_sendRawTransaction)**： 当你转账时，你把签好名的交易发给 RPC 节点。RPC 节点会把这笔交易\*\*广播\*\*给它相连的其他 P2P 节点，最终被矿工打包进区块。

\### Solidity 编程：更严格的“Java”

Solidity 是静态类型语言，语法有点像 JavaScript 和 C++ 的混合，但逻辑上更接近 Java 的强类型思维。

• **合约结构**`contract MyContract {}` 类似于 Java 的 `class MyContract {}`。它也有构造函数 `constructor()` 和状态变量。

• **状态变量 (State Variables)**：定义在函数外的变量，\*\*永久存储在区块链上\*\*。修改它们需要消耗 Gas。

• **修饰符 (Modifiers)**：类似 Java 的 AOP（面向切面编程）或注解。例如 `onlyOwner`，在执行函数前先检查权限。

• **事件 (Events)**：这是 Web3 的“日志系统”。合约触发 `emit Event()`，前端或索引器监听这些日志来更新 UI，而不是轮询数据库。

### **Java开发者的思考**

• **Map 的区别**：Solidity 里的 `mapping(address => uint)` 类似于 Java 的 `HashMap`，但它\*\*无法遍历\*\*（没有 `.keys()` 或 `.size()`）。如果你需要遍历数据，必须自己维护一个数组来记录 Key。

• **异常处理**：没有 `try-catch` 这种复杂的捕获机制。主要是 `require(condition, "Error Msg")`，一旦失败，整个交易回滚 (Revert)，就像数据库事务回滚一样。
<!-- DAILY_CHECKIN_2026-01-20_END -->

# 2026-01-18
<!-- DAILY_CHECKIN_2026-01-18_START -->




## 1\. **Web2 开发者向 Web3 转型** （Day 2: Wallets, Mnemonics, Keypairs）

### 账户本质

看视频主要需要理解区块链账户到底是什么。 与传统银行账户需要申请和审批不同，在以太坊上创建账户是完全\*\*无许可（Permissionless）\*\* **确定性 (Deterministic)** 的。只要你有那12个单词，无论在哪里恢复，都能按顺序生成 Account 1, Account 2... 这种索引关系永远不会变

### 概率与信任

关于“会不会被暴力破解”的疑问，这涉及到我们如何信任数学。 我们的“穴居人大脑 (Caveman Brain)”可能会觉得猜出12个单词很容易，但数学告诉我们要信任概率。猜中私钥的难度相当于每次洗扑克牌都洗出完全一样的顺序。 即使动用全世界的算力每秒猜测一万亿次，猜中有效私钥所需的时间也超过宇宙目前的寿命。所以，与其担心被暴力破解，不如担心自己把助记词泄露给钓鱼网站或保存在联网的设备上。

### 资产托管进阶

钱包使用的几个阶段：

L1 **托管 (Custodial)**：像 Coinbase/币安。资产只是中心化数据库里的一个数字，你并不真正拥有它（Not your keys, not your coins），但适合新手入门。

L2 **非托管 (Non-Custodial)**：像 MetaMask。你自己掌握私钥，拥有绝对控制权，但也承担绝对责任（弄丢就完了）。

L3 **智能合约钱包 (Smart Contract Wallet)**：像 Gnosis Safe 或 Argent。这是未来的方向，通过多重签名 (Multisig) 或“监护人”机制来实现社交恢复。即使丢了私钥，也能通过家人或备用设备找回，解决了单点故障的焦虑。

作为 Web3 初学者，核心要点是从用户转变为拥有者。你不需要任何机构的许可，只需通过数学生成一串随机数即可创建账户，但这同时也意味着你必须独自承担安全责任——助记词一旦丢失或泄露，资金将永久无法找回，因为区块链世界没有客服。入门的最佳实践是下载 MetaMask，学会使用 **Etherscan**（无私的第三方）来验证链上事实，并在存入真实资金前进行“自杀式测试”\*\*（删除钱包并尝试通过助记词恢复），以此真正理解“做自己的银行”的含义。
<!-- DAILY_CHECKIN_2026-01-18_END -->

# 2026-01-17
<!-- DAILY_CHECKIN_2026-01-17_START -->





## 1\. **Web2 开发者向 Web3 转型的入门课程介绍**（Day 1 Intro）

### 整体概念

看视频还是主要了解一下以太网的整体概念。

与普通中心化不同的是，区块链是一个由成千上万个节点组成的点对点网络，没有中心控制。智能合约一旦部署，就无法更改（Code is law），每个人都要遵守相同的规则，即使是开发者自己也无法停止它。

它主要解决了信任的问题，你不需要信任别人，你只需要信任代码的逻辑。所有规则都已经写好了，通过正确的规则与激励机制来引导行为。而且由于它是去中心化的，我们中心化的项目需要主从来备份，但是区块链天生就是去中心化的，整个p2p网络就是冗余，每个节点都会保存所有智能合约，任何更新都会同步到所有节点。

### 学习态度

关于学习的态度，这个我觉得也是特别需要学习的。学习的路程肯定不是一帆风顺的，我们不能期待学习过程一直都是“快乐路径”。真实的开发学习，错误、不断的调试以及难以解决的困难才是日常。

一个好的学习方法就是 构建小项目->写好readme->发到笔记、github上展示->重复循环 这样其实也就是我们能够获得正反馈，不断学习进步的一条路径

### 不同学习层次

学习eth的几个层次

L1 就业 掌握基础的技能

L2 创业 解决实际的问题 构建自己的产品 找到获得收入的方式

L3 审计 成为以太坊或者说是区块链专家 这是最难的
<!-- DAILY_CHECKIN_2026-01-17_END -->

# 2026-01-16
<!-- DAILY_CHECKIN_2026-01-16_START -->






## 1\. 阅读021 学习以太坊第 3 章

记录一下比较重要的几个概念：

1\. 账户的几个重要的状态

**Nonce**：计数器。EOA 记录发了多少交易；合约记录创建了多少子合约。这里也就放孩子了重放攻击，如果重复广播，发现就得交易就会失效

**Balance**：余额。持有的 ETH 数量（单位：Wei）。

**StorageRoot**：哈希值。指向该账户的私有数据库（KV 存储）。

**CodeHash**：哈希值。指向该账户的可执行字节码。

2\. 两种账户类型：

EOA：由私钥控制的账号，我们的 okx wallet、metamask 钱包

合约账户：部署在链上的代码逻辑，而且逻辑已经写死，不可篡改。（如果要进行操作，需要使用代码的逻辑来判断 例如：require(msg.sender == admin)）

3\. 资产的形式（这个从 eth scan 上也能看出来）：

1\. ETH：这个是记录到账户的 balance 字段里面的

2\. 代币（ERC-20/NFT） 本质上是通过查询向代币合约的地址查询你的地址，最终得出来你的账户里持有多少代币，而因为你有私钥，你是全网唯一能够修改代币合约 storage 这一行 Entry 权限的人

4\. 账户之间的交流：

外部调用`EOA -> 合约`。需要签名，消耗 Gas，产生顶层交易记录。

内部调用`合约 -> 合约`。通过 `call` 或 `delegatecall` 实现。

阅读完今天的内容，也解答了我之前的疑惑。那就是为什么 okx wallet 之类的钱包，你只有一套助记词，为什么我们可以直接创建另外的小号，但是为什么没有新建新的一套助记词。因为这都是使用一套助记词派生出来的，而且使用一套助记词，按照顺序创建出来的账户都是固定的。同时也理解了为什么在交易所想要快速出售代币的时候，需要找到代币合约的地址，然后通过向合约地址提前授权然后进行交互。因为这些代币根本就不存在你的账户里，只是把你的账户交易的地址记录到合约里。
<!-- DAILY_CHECKIN_2026-01-16_END -->

# 2026-01-15
<!-- DAILY_CHECKIN_2026-01-15_START -->







## 1\. 阅读021 学习以太坊第 2 章

1\. 节点双大脑：执行客户端EL，公式客户端CL

**执行客户端（EL）：** 它是干苦力的。专门负责算账、跑智能合约、处理交易。就像是法院里的书记员，所有的案卷细节、账户余额都在它手里。常见的有 Geth 和 Nethermind。

**共识客户端（CL）：** 它是拿主意的。负责跑 PoS 协议，决定哪条链才是正统，谁有资格出块。它就像是法官，根据书记员提供的材料做裁决。常见的有 Lighthouse 和 Prysm。

2\. 节点之间如何交流的：

**找邻居：** 新节点刚上线时，会先找几个长期在线的引导节点要一份通讯录，然后通过一种叫 Kademlia 的算法，在网络里一传十、十传百，直到把自己的好友列表填满。

**传八卦（Gossip协议）：** 当一个节点收到新交易或新区块时，它不会发给所有人，而是随机告诉几个邻居（不是全部）。邻居再告诉他们的邻居，几秒钟之内，全网就都知道这个八卦了。这种方式非常抗造，就算一部分节点掉线，信息也能传开。

3\. 运行节点的选择：

**全节点（Full Node）：** 绝大多数人的选择。它存了所有的区块数据，但会把很久以前的旧账删掉一些（修剪）来省空间。它能独立验证所有交易，不求人，安全性最高。

**归档节点（Archive Node）：** 巨无霸。它存了从创世第一天到现在所有的历史快照。如果你想查三年前某个账户在某个瞬间有多少钱，只能找它。缺点是太占硬盘，动不动就要十几个 TB。

**轻节点（Light Node）：** 极简版。它只存区块头（相当于书的目录），不存具体内容。想看细节时再去问全节点。适合手机或者配置很低的设备。

4\. 最重要的！数据分析的方法

**Dune：** 重点！只要会写 SQL 就能查全链数据，不用自己维护节点。

**Google BigQuery：** 谷歌已经把以太坊数据整理成了数据库，适合大规模的数据挖掘。

**Etherscan API：** 适合写点小脚本，查查交易记录。

原来以太坊的运行底层还是挺有趣的，同时也了解到了 Dune 这个数据分析平台，之前看到别人统计钱包的交易数据就是使用的这个，图表以及数据统计也写的很精美。之后可以深入学习使用一下，毕竟感觉用的人还是挺多的。
<!-- DAILY_CHECKIN_2026-01-15_END -->

# 2026-01-14
<!-- DAILY_CHECKIN_2026-01-14_START -->








## 1\. 阅读 Web3 实习手册「安全与合规」部分

把这篇文章阅读下来之后，也就理解了为什么国内接触这方面内容为什么会比较难，主要还是合规方面的问题比较多

\- 技术人员不仅仅只是个写代码的，参与代币模型设计、空投逻辑配置、合约部署等环节，都有可能被认定为犯罪行为，不能代码写着写着把自己写进去了

\- 特别是赌场类的项目，充值、抽盲盒、提现的闭环存在，就有可能被认定，说实话，现在的polymarket风险还是挺大的，特别是还搞中文社区。

\- 链上工作工资用 U，劳动仲裁、出金也存在一些问题，甚至有用代币的，波动比较大

\- 面试避免下载第三方面试工具，注意保护自己的钱包，省得被植入木马

\- OTC 其实问题还是挺多的，如果对方的钱是电信诈骗或者赌博来的。收了这笔钱，你的卡被冻结都是轻的，搞不好还得去局里解释这钱是怎么来的。

\- 监控剪贴板内容，一旦检测到钱包地址，自动替换为黑客的地址。前几天就有人只核对了最后几位，然后转错了地址。不过这个一般来说针对巨鲸，小虾米问题不大

\- 浏览器插件尽量少装，那我装了七八个是不是太多了

\- 助记词/私钥只离线保存，绝不截图、上传网盘或发给任何人

主要还是保住本金，接活儿前得摸摸老板的底，别为了那点高薪把前途搭进去。还有就是出金和安全，发 U 看着爽，但卡被冻了、币被钓了，那真是一辈子的积蓄瞬间归零。这行还得自己当自己的保镖和律师，先保证自己“不出事、不进去”，这比什么都强。
<!-- DAILY_CHECKIN_2026-01-14_END -->

# 2026-01-13
<!-- DAILY_CHECKIN_2026-01-13_START -->









## 1\. 阅读021 学习以太坊第 1 章

\- 什么是以太坊？一个**去中心化、开源、支持智能合约**的公共区块链平台。

\- 它和核心组成？不只是 ETH 代币这么简单的概念，而是有智能合约、dApp 以及代币（代币的标准 ERC-20 和 ERC-721 等）

\- ETH 代币不只是用来支付 gas，还是 PoS 质押资产，生态资产底座（比如说很多 NFT 都是使用 ETH 来计价）

\- Gas 的计价： **Gas Fee ≈ (Base Fee + Priority Fee) × Gas Used**

\- **baseFee**：协议根据拥堵自动调节，\*\*执行时直接销毁\*\* 矿工主要收入是 PoS 的质押奖励 以及可能的 MEV

\- 与比特币的对比

\- BTC 价值存储，这就是为什么会出现囤 BTC 的思想

\- ETH 主要体现在生态方面，出块速度快，也就代表了速度快 可编程的特性有了更多的可能性

\- dApp 和普通 Java 后端的区别（一个后端程序员的思维）

\- 一个是部署在 EVM 上，一个是跑在特定的服务器上。

\- 数据就是链上的数据，一个是存在数据库里。

\- 链上我的地址就是我的身份，采用私钥来验证，私钥丢了，谁也帮不了你；采用权限管理。

\- 写入=发交易 需要等待打包和确认 速度慢，写入数据库就代表完成来

\- dApp 合约部署之后不可变 所需要的测试 审计，Java 可以发版回滚来修复

\- dApp 由用户来支付 Gas 而程序员需要优化 Gas，费用估计等，Java则是由服务商来承担

\- L2 的出现，支持来大量交易的执行，提升来 TPS，降低来交易费用

阅读完这一章，感觉以太坊不再是我之前简简单单了解的 ETH 这么简单，不再知识价格上上下下波动的那么肤浅。它其实是一台世界计算机，ETH 只是它的原生燃料和核心资产之一。它真正厉害的地方在于智能合约：规则写进代码里，上链以后大家都按同一套规则玩，这也是 dApp 能跑起来、代币标准（ERC-20、ERC-721）能形成生态的原因。我也理解来支持 ETH 价格上涨的理由，他是一个跑应用的重要基础设施。
<!-- DAILY_CHECKIN_2026-01-13_END -->

# 2026-01-12
<!-- DAILY_CHECKIN_2026-01-12_START -->










## 1.观看分享会 - 以太坊中文周会 260112

![CleanShot 2026-01-12 at 14.23.14@2x.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/BRabb1t/images/2026-01-12-1768203508701-CleanShot_2026-01-12_at_14.23.14_2x.png)

主要还是一些关于加密行业的发展预测，2025年成为了市场最好也是最坏的一年，比特币等资产虽然创下新高，但整体没有带来大规模上涨。这也就说明市场向基础面转型，项目必须证明实用性，这点让我认识到Web3像传统行业一样，需要长期构建，而不是短期的炒作。关键的几个观点如下：

-   以太坊会在26年迎来复兴，而RWA的发展将大有所为。RWA是将真实世界资产代币化，比如债券上链用于借贷。
    
-   而Sol活跃度可能会迎来降低，各个平台的竞争带来活跃度的变化，每条链还需要找到自己的定位并且朝着各自擅长领域发展。
    
-   Defi这个方面可以重点关注，因为稳定币的快速发展，大量资金需要获取低风险收益。
    
-   行业的发展离不开技术的不断进步，在关注行业大方向的时候，也需要学习技术的原理，来更好地预测未来的趋势。
    

## 2.创建钱包，并尝试测试币转账

![CleanShot 2026-01-12 at 15.52.01@2x.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/BRabb1t/images/2026-01-12-1768204352301-CleanShot_2026-01-12_at_15.52.01_2x.png)

创建了Metamask的钱包，专门用于开发和学习。并且领取测试币（[https://cloud.google.com/application/web3/faucet/ethereum/sepolia](https://cloud.google.com/application/web3/faucet/ethereum/sepolia)），同时发送给填写了共享表格地址的小伙伴，区块链浏览器转账记录（[https://sepolia.etherscan.io/tx/0xc324ca837efab433d7829e4a5ef9c4109a965f07f61c23f42954384533e54871](https://sepolia.etherscan.io/tx/0xc324ca837efab433d7829e4a5ef9c4109a965f07f61c23f42954384533e54871)）

![CleanShot 2026-01-12 at 15.57.25@2x.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/BRabb1t/images/2026-01-12-1768204656022-CleanShot_2026-01-12_at_15.57.25_2x.png)

## 3.在 My First NFT mint 第一个 NFT

![CleanShot 2026-01-12 at 16.06.45@2x.png](https://raw.githubusercontent.com/IntensiveCoLearning/Web3_Internship_Bootcamp_2026_Winter/main/assets/BRabb1t/images/2026-01-12-1768205226037-CleanShot_2026-01-12_at_16.06.45_2x.png)

也是拥有了第一个nft，感谢lxdao的教程和支持！

## 4.Web3 实习手册\[入门导读\]部分

-   记账在之前是银行或者平台自己存储，区块链更像把账本复制很多份，分散到一堆节点手里，每个人都存一份一样的。这样就不会出现某个中心说你没钱你就没钱那种单点控制的感觉。
    
-   区块就是一段时间内打包的一堆交易记录，链就是把区块按时间顺序串起来的结构。后面的区块保存了前面的摘要，所以前面的内容就不能修改了。也就达到了不可篡改的母的。
    
-   而匿名则是因为地址并不能对应谁是谁，除非你公开承认，不然只能知道是哪个地址进行了操作，而不是哪个人。
    
-   51%的机制保证了可信，因为要改变一半以上的节点非常困难
    

在理解了这些基本内容之后，web3对于我的吸引更加强烈了。虽然在有些方面对比中心化来说存在不足，比如交易速度、安全和治理风险、用户体验等。但是，它的开放创新，各种先进的理念以及用户对于自己资产的完全掌控，还是具有非常强的吸引力的。相信在不久的将来，web3能够在生活中带来更多的改变。
<!-- DAILY_CHECKIN_2026-01-12_END -->
<!-- Content_END -->
